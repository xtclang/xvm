/**
 * A collection of metrics produced by a single instrumentation scope.
 */
const ScopeMetrics(InstrumentationScope scope,
                   Metric[]             metrics   = [],
                   String?              schemaUrl = Null) {}
