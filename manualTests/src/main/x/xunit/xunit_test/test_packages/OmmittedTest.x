/**
 * A simple test class where all methods are omitted
 * or are not tests.
 */
class OmittedTest {
    @Test(Test.Omit)
    void testOne() {
    }

    @Test(Test.Omit)
    void testTwo() {
    }

    void noTest() {
    }
}