module TestSimple
    {
    @Inject Console console;

    void run()
        {
        console.println(Base.value);
        }

    class Base(Int value0)
        {
        Int value0;

        static Int value =
            {
            return value0;
            };
        }
    }
