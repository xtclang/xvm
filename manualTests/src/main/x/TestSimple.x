module TestSimple {
    @Inject Console console;

    void run() {
        class Test() {
        }

        class Test() { // the repetitive class name inside a method used to assert the compiler
        }
    }
}
