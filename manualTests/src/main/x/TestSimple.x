module TestSimple
    {
    @Inject Console console;

    void run()
        {
        Int value = TODO("value");
        function Int (Int) fn = (i) ->
            {
            return value;
            };
        }
    }