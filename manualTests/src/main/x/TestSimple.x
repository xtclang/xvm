module TestSimple
    {
    @Inject Console console;

    void run()
        {
        }

    Int test(Int id)
        {
        switch (id)
            {
            default:
                break;
            }

        return id;
        }
    }