<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logback configuration for XTC DSL generator tools.
  Keep output minimal by default - only warnings and errors.

  Override via: -Plog=DEBUG, -Dxtc.logLevel=DEBUG, or XTC_LOG_LEVEL=DEBUG
-->
<configuration>
    <!-- Suppress logback's own status messages -->
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <appender name="STDERR" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.err</target>
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level %logger{0} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- XTC tooling logs - WARN by default -->
    <!-- Precedence: system property > environment variable > default -->
    <variable name="LOG_LEVEL" value="${xtc.logLevel:-${XTC_LOG_LEVEL:-INFO}}"/>
    <logger name="org.xtclang" level="${LOG_LEVEL}"/>

    <!-- Quiet root logger -->
    <root level="ERROR">
        <appender-ref ref="STDERR"/>
    </root>
</configuration>
