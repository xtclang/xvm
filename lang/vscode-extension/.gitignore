# npm
# package-lock.json IS tracked — it pins transitive deps so `npm ci` produces
# a reproducible tree on every machine and so `npm audit fix` results survive
# in git. This is npm's documented recommendation for applications (vs.
# libraries published to the registry).
node_modules/
.vscode-test/

# TypeScript compiled output
out/

# Packaged extension
*.vsix

# Generated/copied files from Gradle build
language-configuration.json
syntaxes/xtc.tmLanguage.json
server/
icons/
