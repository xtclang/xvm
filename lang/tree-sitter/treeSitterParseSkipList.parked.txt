# Tree-sitter parse-test skip list — PARKED grammar gaps.
#
# Companion to `treeSitterParseSkipList.intentional.txt`. Both are
# loaded by `:lang:tree-sitter:testTreeSitterParse`. The split exists
# so the real "grammar debt" (this file) isn't drowned out by files
# that are permanently excluded from the XTC compile.
#
# Files listed here are valid Ecstasy code that the reference compiler
# DOES parse, but our tree-sitter grammar currently doesn't. Each one
# represents real work to do. When a parked entry is fixed, remove
# the line and re-run the sweep; the file should then parse cleanly.
#
# One path per line, relative to the composite-build root (the
# directory that contains lib_*/ and manualTests/). Blank lines and
# `#` comments are ignored.

# reflect.x and generics.x both parse cleanly now -- both removed from
# this list. The two fixes that closed them out:
#
# 1. C5 LHS via split shift operators: `<<` / `>>` / `>>>` are emitted
#    as `seq('<', token.immediate('<'))` etc. so the lexer never
#    produces a multi-char shift token. This unblocks the LHS of
#    `Function<<Int, String>, <Int>> f = ...`. Closing side already
#    handled by the TYPE_GT external scanner (commit d384f2394). Cost:
#    a 1-element self-conflict on `binary_expression`.
#
# 2. Template interpolation expressions can span multiple physical
#    lines via the parent multiline template's `|` continuation. The
#    scanner now also handles trailing whitespace before the closing
#    `}` of an interpolation -- forms like `$"{y } abc"` work -- and
#    emits a MULTILINE_CONTINUATION extras token covering `\n + hspace
#    + |` when persistent state shows we're inside a multiline-template
#    interpolation expression (depth counter bumped on
#    MULTILINE_EXPR_START, popped on TEMPLATE_EXPR_END). Without this
#    the `|` got lexed as bitwise-OR, breaking method chains like
#    `{method.as(Type).\n   |   invoke()}`.
#
# 3. literals.x: bare absolute-path primary expression
#    (`Path p = /allow.txt;`) closed by adding the existing
#    `path_literal` rule to `_literal`'s choice and tightening the
#    regex from `/\/[\w\-./]*/` to `/\/[a-zA-Z0-9_][a-zA-Z0-9_\-\.\/]*/`
#    -- the first body char must NOT be `/` or `*`. The earlier
#    parking note assumed the internal regex would conflict with
#    spaceless division (`bytesLen/2`); empirically it does not,
#    because tree-sitter's lex states are state-restricted and a
#    primary-expression starter is not active mid-binary-expression.
#    The real conflict was with `//` (line_comment) and `/*`
#    (block_comment) start tokens, which the stricter first-char
#    class disambiguates. Cost: zero new conflicts, no scanner work.
#
# 4. callTests.x: `(i1, Int i2) = testMultiReturns(0);` -- mixed
#    bare-ident + typed-decl in tuple_assignment. Closed by
#    combining (a) an external-scanner TUPLE_ASSIGN_LPAREN token
#    that peeks past balanced `(...)` for `=` and forces LR commit
#    to tuple_assignment at the `(` shift, with (b) narrowing the
#    bare-element alt of `tuple_assignment_element` from the full
#    `prec.dynamic(1, $._expression)` to the explicit shape
#    `choice($.identifier, $.member_expression, $.index_expression)`.
#    The narrow alt is the key that makes the prior parked attempt
#    (TUPLE_ASSIGN_LPAREN alone) finally work -- with full
#    `_expression` available, typed elements like `UInt8 left` were
#    losing to a failing _expression parse (no backtrack once GLR's
#    container-level fork is suppressed by the scanner). Restricting
#    the bare alt to the three node shapes that actually appear as
#    bare LHS slots in real code (single ident, `this.foo` chain,
#    `xs[i]` subscript) lets the typed alt win cleanly when present
#    while still accepting all observed bare-ident cases. Scanner
#    additionally has to skip leading whitespace via
#    `lexer->advance(skip=true)` (tree-sitter doesn't auto-skip
#    extras before invoking external scanners) AND must guard against
#    eating the hspace after a `TODO` keyword by checking
#    `!valid_symbols[TODO_FREEFORM_*]` -- otherwise it consumes the
#    whitespace the TODO scanner needs for `is_hspace(peek)` and
#    breaks ~16 files with `TODO foo bar` text. Sweep 876/876.
#
# Sweep is at 880/880 with 13 parked entries (newly uncovered, see below).

# === Newly-uncovered grammar gaps (formerly hidden in intentional.txt) ===
#
# The `intentional.txt` skip list previously held 19 files under the
# rationale "intentionally not parseable by tree-sitter". Re-checking
# revealed the categorization was wrong:
#   - 4 files parse cleanly with both tree-sitter and the Java
#     reference compiler -- moved into the active sweep.
#   - 2 files (sock-shop/socks-catalog.x, sock-shop/socks-user.x) have
#     genuine PARSER-NN errors from the Java compiler too; remain in
#     intentional.txt with a corrected header.
#   - The 13 below are real tree-sitter grammar gaps -- the Java
#     compiler parses them cleanly (only emits semantic / verify
#     errors), but tree-sitter produces ERROR nodes. These should be
#     triaged in a follow-up PR; the goal is to get them all into
#     the active sweep just like the originally-parked entries above.
#
# Each entry is annotated with the tree-sitter ERROR-node count from
# the time of un-parking, as a rough proxy for triage size.

manualTests/src/main/x/archive/AddressBookDB.x                              # 2 errors
manualTests/src/main/x/archive/addressDB_imdb/module.x                      # 3 errors
manualTests/src/main/x/archive/addressDB_json/module.x                      # 3 errors
manualTests/src/main/x/archive/sock-shop/socks-cart.x                       # 2 errors
manualTests/src/main/x/dbTests/PeopleTest.x                                 # 3 errors
manualTests/src/main/x/jsondb/jsondb_test.x                                 # 2 errors
manualTests/src/main/x/jsondb/jsondb_test/storage/JsonMapStoreConfigTest.x  # 12 errors
manualTests/src/main/x/jsondb/jsondb_test/storage/JsonMapStoreTest.x        # 16 errors
manualTests/src/main/x/jsondb/jsondb_test/test_db/TestClient.x              # 19 errors
manualTests/src/main/x/jsondb/jsondb_test/tx_manager/TxManagerConfigTest.x  # 3 errors
manualTests/src/main/x/jsondb/jsondb_test/tx_manager/TxManagerLogTest.x     # 7 errors
manualTests/src/main/x/webTests/oauth.x                                     # 2 errors
manualTests/src/main/x/errors.x                                             # 4 errors
