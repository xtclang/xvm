package org.xvm.cc_explore;

import org.xvm.cc_explore.cons.*;

class MethodPart extends Part {
  MethodPart( Part par, int nFlags, IdCon id, CondCon con, FilePart X ) {
    super(par,nFlags,id,con,X);
    throw XEC.TODO();
  }
}
