/**
 * A FixedSizeArray is a specialized implementation of array that supports element replacement
 * in-place, but all other forms of modifications are accomplished by creating modified copies of
 * itself.
 */
class FixedSizeArray<ElementType>
        extends Array<ElementType>
    {
    // TODO
    }