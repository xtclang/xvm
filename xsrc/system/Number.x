value Number
    {
    @ro Sign Sign;

    @ro int BitCount;
    bit[] to<bit[]>();

    Integer to<Integer>();
    Integer8 to<Integer8>();
    Integer16 to<Integer16>();
    Integer32 to<Integer32>();
    Integer64 to<Integer64>();
    Integer128 to<Integer128>();
    UnsignedInteger8 to<UnsignedInteger8>();
    UnsignedInteger16 to<UnsignedInteger16>();
    UnsignedInteger32 to<UnsignedInteger32>();
    UnsignedInteger64 to<UnsignedInteger64>();
    UnsignedInteger128 to<UnsignedInteger128>();
    Float16 to<Float16>();
    Float32 to<Float32>();
    Float64 to<Float64>();
    Float128 to<Float128>();
    Decimal32 to<Decimal32>();
    Decimal64 to<Decimal64>();
    Decimal128 to<Decimal128>();
    }
