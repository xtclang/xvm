/**
 * This package provides the actual storage functionality for the different database types.
 */
package storage {
    /**
     * The injection name prefix for all JSON DB storage configuration values.
     */
    static String ConfigStoragePrefix = jsondb.ConfigPrefix + ".storage";
}