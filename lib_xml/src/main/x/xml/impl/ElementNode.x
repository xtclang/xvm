class ElementNode(String name)
        implements Element
        incorporates Node {

    // ----- constructors --------------------------------------------------------------------------

    /**
     * Construct a new mutable `ElementNode`, copying the content of the passed ElementNode.
     *
     * @param that  the `ElementNode` to copy
     */
    protected construct(ElementNode that) {
        TODO
    }

    // ----- Element API --------------------------------------------------------------------------

    // ----- internal ------------------------------------------------------------------------------

}