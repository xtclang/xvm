# Runtime Metadata And Op Cache Classification

This audit covers the remaining runtime metadata caches and decoded `Op` link/address caches called
out by the reentrancy backlog, plus the follow-up scope around `ObjectHandle.cloneAs(...)`,
`GenericHandle.maskAs(...)`, `RefHandle` view backing, same-owner access views, and cross-owner guard
status.

Inputs read:

* `docs/reentrancy/must-audit-backlog.md`
* `docs/reentrancy/manual-lazy-cache-audit.md`
* `docs/reentrancy/state-inventory.md`
* `docs/reentrancy/runtime-ownership-hardening-ledger.md`
* `docs/reentrancy/fixed-in-this-branch.md`
* `docs/reentrancy/clone-usage-audit.md`

Status labels:

* `MUST FIX`: source inspection shows shared runtime state can be corrupted or owner-contaminated.
* `MUST AUDIT`: source shape is unsafe or unproven for shared/runtime publication, but the runtime
  reachability or invariant still needs a proof.
* `SHOULD FIX`: low-risk unsafe publication or mutability where failure is expected to be duplicate
  work or identity churn, but a small hardening change would remove the risk.
* `DONE`: this branch already contains the hardening or an adequate proof plus tests.
* `PROVEN CONFINED`: the state is compile-time, assembly-time, JIT-build-time, or otherwise not
  reachable from shared decoded runtime objects.

## Top Conclusions

1. `JumpVal` and `JumpVal_N` are fixed in this branch. The old runtime switch
   caches were removed from the shared decoded `Op` objects and replaced by
   `Container`-owned runtime-op cache entries keyed by `(decoded op, cache
   category)`. The cached tables still contain the same case-handle arrays,
   jump maps, wildcard/range masks, algorithm choices, and frame-derived
   `TypeConstant` data; the difference is that each executing container builds
   and reuses its own table instead of letting the first container in the JVM
   publish its owner state globally.
2. `JumpNFirst.m_fVisited` is fixed in this branch. The opcode is emitted for
   `assert:once`, and both `AssertStatement.isOnlyOnce()` and the opcode
   definition describe "first time execution reaches this op" semantics. The
   owner key therefore remains the decoded op, but the state is now a final
   `AtomicBoolean` so concurrent first execution has exactly one winner.
3. Same-owner `GenericHandle` access-view backing is fixed in this branch. The old
   `GenericHandle.cloneAs(...)` shallow-cloned the handle, shared the final field array, and rewired
   inflated `RefHandle.OUTER` entries in that shared array (`ObjectHandle.java:62`,
   `ObjectHandle.java:497`, `ObjectHandle.java:503`, `ObjectHandle.java:512`,
   `ObjectHandle.java:649`). That meant creating one same-owner access view could mutate the source
   view. The replacement keeps the shared field array for regular values and adds sparse per-view
   overrides only for inflated refs whose `$outer` is view-specific.
4. Pure `Op` address/link fields are not runtime-lazy, but they are not yet proven safe by eager
   linking before publication. `Code` resolves addresses in the constructor and during reassembly
   (`MethodStructure.java:2188`, `MethodStructure.java:2192`,
   `MethodStructure.java:2669`, `MethodStructure.java:2683`), while `MethodStructure.ensureCode()`
   publishes `m_code` through a plain transient field (`MethodStructure.java:608`,
   `MethodStructure.java:615`, `MethodStructure.java:3118`). Without a documented pre-publication
   warmup guarantee or a safe-publishing `volatile`/synchronized owner cell, the address/link group
   stays `MUST AUDIT`.

## Runtime Handle View Caches

| Status | Cache/source | Owner, key, invalidation | Thread-safety and reachability | Failure mode, required proof, and proper fix |
| --- | --- | --- | --- | --- |
| `DONE` | Cross-owner direct masking guard in `GenericHandle.maskAs(...)` (`ObjectHandle.java:563`, `ObjectHandle.java:576`, `ObjectHandle.java:578`, `ObjectHandle.java:600`). | Owner is the source handle graph and target `Container`; key is `(target owner, mask type)`. No invalidation; masking is an access-view operation. | Guard checks `owner != ownerOrig && !isShared(owner, null)` before `cloneAs(...)`. Runtime reachable. | Fixed highest-risk cross-owner clone path. Keep `OwnershipDiagnosticsTest.crossOwnerMaskRejectsNonSharedHandleBeforeClone()` (`OwnershipDiagnosticsTest.java:145`) and owner diagnostics that fail if a non-shared handle graph is shallow-cloned across owners. |
| `DONE` | Same-owner `GenericHandle.cloneAs(...)` view backing and `RefHandle.OUTER` rewiring (`ObjectHandle.java:497`, `ObjectHandle.java:503`, `ObjectHandle.java:506`, `ObjectHandle.java:512`, `ObjectHandle.java:649`). | Owner is the same `Container`; key is the view `TypeComposition` and access mode. No invalidation. The field backing represents one runtime object across multiple access views. | Base `ObjectHandle.cloneAs(...)` remains a shallow view primitive, but `GenericHandle` no longer mutates inflated refs in the shared array. The common path still reads and writes `m_aFields` directly. Struct/revealed view clones install a copy-on-write sparse override array only for inflated refs that need a view-local `$outer`. Runtime reachable through `ClassComposition.ensureAccess`, `PropertyComposition.ensureAccess`, `CanonicalizedTypeComposition.ensureAccess`, `xRef.maskAs`, and `GenericHandle.maskAs`. | Fixed. The old practical failure was that creating a same-owner access view rewrote refs in the source view, so a previously obtained `Ref` could report the wrong holder. Deep-copying the whole field array was rejected because regular field writes must remain visible through all same-owner views. The regression `GenericHandleCloneAsTest.sameOwnerCloneKeepsInflatedRefOuterViewLocal()` fails on master by observing the source ref's `$outer` change to the clone, and passes here while also proving referent and regular-field write-through behavior. Performance shape is preserved: no recursive graph copy, no per-handle allocation for the no-inflated-ref/no-view-override case, and at most one sparse override array plus one ref view per affected inflated field. |
| `MUST AUDIT` | Base `ObjectHandle.cloneAs(TypeComposition)` and clone overrides (`ObjectHandle.java:62`, `Proxy.java:424`, `CanonicalizedTypeComposition.java:74`, `PropertyComposition.java:123`). | Owner/key depend on subclass. For `GenericHandle`, key is access view. For immutable/native handles, clone may be only a type view. Invalidation is none. | Shallow clone has no synchronization and copies all subclass fields unless overridden. Runtime reachable from access-view paths and constant relocation. | Audit every override and owner-bearing subclass field. Proper fix is to restrict base `cloneAs` to immutable/view-only subclasses by contract, and require explicit view-backing or owner-rekey logic for mutable or owner-bearing handles. Required diagnostic: reflective clone audit that flags non-final owner-bearing fields copied by `Object.clone()` without an override. |
| `MUST AUDIT` | `ConstHeap.relocateConst(...)` fallback clone (`ConstHeap.java:212`, `ConstHeap.java:232`, `ConstHeap.java:235`). | Owner is the target `ConstHeap`/`Container`; key is the target-pool `Constant`. Invalidation is map removal when a shared parent heap owns the constant. | `ConcurrentHashMap.computeIfAbsent` protects heap insertion, but the fallback uses `cloneAs(...)` when the constant handle composition belongs to another container. Runtime reachable through constant relocation, including switch-cache paths. | Safe only if every relocated handle is immutable/shared and `cloneAs` is a view relabel, not owner transfer. Proper fix is to either prove relocated constants contain no mutable owner-bearing graph, or introduce a real const relocation/copy path. Required stress: relocate a const with inflated refs or nested handles across parent/child containers under ownership validation. |
| `DONE` | `RefHandle.createRegisterRef(...)` construction lifecycle (`xRef.java:848`, `xRef.java:869`, `xRef.java:883`). | Owner is the current `Frame.VarInfo`; key is register index. Invalidation is frame lifetime. | First register ref is cached only after constructor return. Second ref delegates to cached first ref. Frame-local reachability only. | Construction escape fixed. Keep shape test that `Frame.VarInfo` cannot observe a partially constructed `RefHandle`. This does not solve same-owner inflated-ref view backing. |

## Decoded Op Runtime Caches

| Status | Cache/source | Owner, key, invalidation | Thread-safety and reachability | Failure mode, required proof, and proper fix |
| --- | --- | --- | --- | --- |
| `DONE` | `JumpVal` runtime switch cache: former `m_ahCase`, `m_mapJump`, `m_algorithm`, `m_typeCond`, and range lists. | Owner is now the executing `Container`; key is `(decoded op identity, SWITCH cache category)` in `Container.f_mapRuntimeOpCache`. Invalidation is container lifetime, matching the old no-invalidation behavior for decoded op caches while making the owner explicit. | `buildJumpMap(...)` still builds the same `ObjectHandle[]`, jump map, range list, algorithm, and condition type from the current frame. It now publishes an immutable `SwitchCache` through `Container.putRuntimeOpCacheIfAbsent(...)` instead of writing several fields on the shared op. `JumpIsA` consumes the same cached case-handle array through `SwitchCache.cases()`. | Fixed. The old synchronized field writes made only the first build atomic; they did not make the cache owner-correct. The new shape can duplicate first-build work under a same-container race, but the winner is reused and no later execution can see another container's handles or type constants. Performance/semantics are preserved: the hot path still hits a cached switch table, the table representation is unchanged, and the one-container footprint is roughly the old table plus the container cache entry. Multi-container footprint grows per executing container by design because owner-bearing handles cannot be shared safely. Guards: `OpRuntimeCacheTest.switchOpsDoNotCacheOwnerValuesOnDecodedOps()` fails on master because these owner-bearing fields still exist on `JumpVal`, and `RuntimeTest.runtimeOpCacheIsContainerLocalAndTyped()` proves the new container cache reuses the first same-owner table while keeping a second container separate. |
| `DONE` | `JumpVal_N` runtime switch cache: former `m_aahCases`, `m_atypeColumn`, `m_amapJumpSmall`, wildcard/range lists, `m_aAlgorithm`, and `m_algorithm`. | Owner is now the executing `Container`; key is `(decoded op identity, SWITCH cache category)` in `Container.f_mapRuntimeOpCache`. The cache records the same column shape and small-switch maps that the op-global fields used to record. | `explodeConstants(...)` still resolves the row/column case handles through the current frame and relocates const handles through the executing container. `buildSmallJumpMaps(...)` still builds the per-column maps, wildcard masks, range lists, type columns, and algorithm array, but returns an immutable `SwitchCache` published by the container. | Fixed. The old code assigned `m_aahCases` before synchronized map construction and then stored all per-column owner state on the decoded op. A later container could reuse maps keyed by the first owner's handles. The new shape keeps identical switch matching behavior and hot cached lookup behavior, but the cached table belongs to the container that produced the handles. Guards: `OpRuntimeCacheTest.switchOpsDoNotCacheOwnerValuesOnDecodedOps()` fails on master because these owner-bearing fields still exist on `JumpVal_N`, and `RuntimeTest.runtimeOpCacheIsContainerLocalAndTyped()` proves the shared cache API is owner-local and typed. Follow-up stress should add an executable multi-column switch module with wildcard/range cases to exercise the behavior under two containers; the source-shape guard closes the known broken cache field pattern. |
| `DONE` | `JumpNFirst.m_fVisited` (`JumpNFirst.java:45`, `JumpNFirst.java:75`). | Owner/key is deliberately the decoded op instance. `JMP_NFIRST` is emitted for `assert:once`, and the language/compiler documentation says the assertion is evaluated only the first time execution reaches that instruction. Invalidation is never, matching the old behavior. | The old plain boolean was a data race. The new field is a final `AtomicBoolean`, and `process(...)` uses `compareAndSet(false, true)` so concurrent first execution has exactly one fall-through winner. | Fixed. This intentionally does not move state to the `Container` runtime-op cache because that would make `assert:once` run once per container and change behavior. `OpRuntimeCacheTest.jumpNFirstUsesAtomicDecodedOpState()` guards the field shape and sequential semantics; `OpRuntimeCacheTest.jumpNFirstConcurrentFirstExecutionHasOneWinner()` proves the parallel first-execution behavior. |
| `MUST AUDIT` | Pure address/link and scope simulation caches: `Op.m_lStruct`, `OpJump.m_opDest/m_cExits`, `OpCondJump.m_opDest/m_cExits`, `LoopEnd.m_opDest`, `OpSwitch.m_aOpCase/m_opDefault/m_acExits/m_cDefaultExits`, `JumpInt` equivalents, `GuardStart.m_aOpCatch`, `Jump`/`OpReturn` finally metadata (`Op.java:118`, `OpJump.java:52`, `OpJump.java:123`, `OpCondJump.java:166`, `OpCondJump.java:743`, `LoopEnd.java:38`, `OpSwitch.java:106`, `OpSwitch.java:453`, `JumpInt.java:85`, `JumpInt.java:251`, `GuardStart.java:111`, `GuardStart.java:212`, `Jump.java:63`, `Jump.java:174`, `OpReturn.java:37`). | Owner is `MethodStructure.Code` and its decoded op array; key is method body version. Invalidation occurs when code is reassembled, dead code is removed, or `m_code` is reset. | Not runtime-lazy: `Code` sets `m_aop`, calls `addressAndSimulateOps`, then `resolveCode` in the constructor for deserialized code (`MethodStructure.java:2188`, `MethodStructure.java:2192`). Reassembly also calls `addressAndSimulateOps` (`MethodStructure.java:2536`, `MethodStructure.java:2619`). But `MethodStructure.ensureCode()` and `Code.m_aop` publication are plain, not volatile/final/synchronized (`MethodStructure.java:608`, `MethodStructure.java:615`, `MethodStructure.java:2718`, `MethodStructure.java:3118`). Frames take `function.getOps()` directly (`Frame.java:133`). | These caches are safe by eager linking only if all runtime-reachable methods are linked and safely published before frames can execute them, or if `ensureCode()` is never concurrently first-called after method publication. That proof is not present in source. Practical failure is partially visible link state, duplicate code objects with divergent op arrays, null branch targets, wrong exit counts, or incorrect guard/finally cleanup. Proper fix: safely publish a fully linked immutable `ResolvedCode` snapshot via synchronized/volatile/atomic owner state, or make container publication eagerly link every runtime method and add a diagnostic that throws if `resolveAddresses` runs after publication. Required stress: parallel first `getOps()`/first execution on methods containing jumps, loops, switches, guards, and finally; assert all op link fields are resolved before PC execution and `resolveAddresses` counters remain zero during runtime. |
| `MUST AUDIT` | `GuardStart.m_guard` and `GuardAll.m_guard` cached runtime guard structs (`GuardStart.java:142`, `GuardStart.java:145`, `GuardStart.java:216`, `GuardAll.java:67`, `GuardAll.java:70`, `GuardAll.java:195`). | Owner/key is decoded op plus method-scope shape. Invalidation is none. | Plain lazy write in `process`. Runtime reachable. Values are not owner-bearing handles, but they include `iPC`, scope index, catch offsets, and next-var state. | Likely duplicate-build-only if scope index is method-shape invariant, but this must be proven. Proper fix: build immutable guard descriptors during address resolution and safely publish them with code, or allocate per frame. Required tests: concurrent first execution of guarded methods with identical guard stack behavior and no races under thread sanitizer-style instrumentation. |
| `MUST AUDIT` | `JumpNSample.m_nEvery` (`JumpNSample.java:55`, `JumpNSample.java:61`, `JumpNSample.java:69`). | Current owner/key is decoded op. It assumes the operand value is invariant for the method/op. Invalidation is none. | Plain runtime write from the first `Frame` value. Runtime reachable. | If the operand can differ by invocation or owner, the first value permanently changes later behavior. Even if invariant, publication is unsynchronized. Proper fix: encode/prove the value as a method constant before runtime, recompute per execution, or key by the correct owner. Required verifier proof or test: execute with different operand values and prove the bytecode verifier rejects it or behavior is per-frame. |
| `DONE` | Removed runtime owner-bearing conditional/common-type caches: former `JumpCond`/`JumpNCond` condition fields and runtime write-back to `OpTest.m_typeCommon` / `OpCondJump.m_typeCommon` (`OpTest.java:216`, `OpTest.java:219`, `OpCondJump.java:226`, `OpCondJump.java:229`). | Owner is current frame only; values are no longer stored on the shared op. | Runtime resolves constants from the current `Frame` without writing them back. | Covered by `OpRuntimeCacheTest.conditionalJumpOpsDoNotCacheFrameConditionalConstants()` and `OpRuntimeCacheTest.commonTypeCalculationDoesNotWriteFrameConstantsBackToOps()` (`OpRuntimeCacheTest.java:60`, `OpRuntimeCacheTest.java:70`). |
| `PROVEN CONFINED` | Assembly/JIT-only op helper state: `Op.Prefix.m_op`, `Op.ConstantRegistry.m_mapConstants/m_aconst`, `Label.m_mapRestore`, `Guarded.m_scope`, JIT-only jump metadata (`Op.java:453`, `Op.java:653`, `Op.java:663`, `Op.java:970`, `Label.java:55`, `Label.java:185`, `Guarded.java:42`, `Guarded.java:58`, `GuardAll.java:197`). | Owner is compiler/assembler/JIT build context. Invalidation is build lifetime. | Not read by interpreter frame execution as shared decoded runtime metadata. `ConstantRegistry.ensureOptimized()` is synchronized for its own build path. | No runtime owner-cache fix needed unless a future change exposes these objects to shared interpreter execution. Keep build/runtime separation assertions. |

## Runtime Composition Caches

| Status | Cache/source | Owner, key, invalidation | Thread-safety and reachability | Failure mode, required proof, and proper fix |
| --- | --- | --- | --- | --- |
| `DONE` | `ClassComposition` derivative composition/call-chain maps: `f_mapCompositions`, `f_mapProxies`, `f_mapProperties`, `f_mapMethods`, `f_mapGetters`, `f_mapSetters` (`ClassComposition.java:76`, `ClassComposition.java:96`, `ClassComposition.java:113`, `ClassComposition.java:123`, `ClassComposition.java:130`, `ClassComposition.java:170`, `ClassComposition.java:253`, `ClassComposition.java:293`, `ClassComposition.java:347`, `ClassComposition.java:390`). | Owner is one `ClassComposition` inception and its `Container`; keys are revealed/proxy/property/method ids. No semantic invalidation after composition publication. | Backed by `ConcurrentHashMap`. Runtime reachable. | Map mechanics are safe. Any remaining concern is not the maps, but late owner-pool registration inside value construction. Keep late-registration diagnostics for runtime-published pools. |
| `MUST AUDIT` | `ClassComposition.m_mapFields`, `m_cRegularFields`, `m_fHasOuter`, `m_fHasSpecial` (`ClassComposition.java:103`, `ClassComposition.java:566`, `ClassComposition.java:572`, `ClassComposition.java:588`, `ClassComposition.java:628`, `ClassComposition.java:690`, `ClassComposition.java:691`, `ClassComposition.java:919`). | Owner is `ClassComposition`/`Container`; key is inception type plus struct field layout. Invalidation is none. | `ensureFieldLayoutImpl` is synchronized and publishes through volatile `m_mapFields`. Side fields are written before the volatile store. Runtime reachable during handle structure initialization and access-view operations. | Needs proof that the `container` parameter can never differ from `getContainer()` in a way that creates owner-specific `FieldInfo`/`TypeComposition` values, and that access-view clone constructors cannot keep a stale copied `m_mapFields` snapshot. Proper fix: make field layout an immutable safely published holder shared by all access views, remove the ambiguous owner parameter or assert it equals the composition owner, and freeze the map. Required tests: same class composition accessed concurrently through inception/protected/struct views, with injected/ref-annotated fields, under owner diagnostics. |
| `MUST AUDIT` | `ClassComposition.m_ashFieldNames` (`ClassComposition.java:433`, `ClassComposition.java:444`, `ClassComposition.java:449`, `ClassComposition.java:970`). | Owner is the composition container; key is field layout. Invalidation is none. | Plain lazy `StringHandle[]`, built with owner-bearing `xString.makeHandle(getContainer(), ...)`. Runtime reachable. | First thread can publish an owner-bearing array without a happens-before edge. Proper fix: precompute with field layout, or use a volatile/final immutable holder and ensure the array never crosses containers. Required stress: concurrent `getFieldNameArray()` on shared const-class compositions with ownership validation. |
| `MUST AUDIT` | `ClassComposition.m_methodInit` auto-structure initializer (`ClassComposition.java:263`, `ClassComposition.java:268`, `ClassComposition.java:271`, `ClassComposition.java:973`). | Owner is class structure plus container pool; key is field layout/struct type. Invalidation is none. | Plain lazy `MethodStructure` creation; runtime reachable when object structures need auto-initialization. | Duplicate initializer creation and late constant-pool/method mutation after runtime publication are possible. Proper fix: synchronize/volatile-publish this cell or precompute before composition publication; assert no runtime-published pool growth. Required test: parallel first instance creation for a class requiring auto initializer with late-registration diagnostics enabled. |
| `MUST AUDIT` | `PropertyComposition.m_clzStruct` (`PropertyComposition.java:61`, `PropertyComposition.java:132`, `PropertyComposition.java:136`, `PropertyComposition.java:380`). | Owner is parent `ClassComposition`/container; key is `Access.STRUCT`. Invalidation is none. | Plain lazy composition object; runtime reachable via `ensureAccess(ObjectHandle, Access)`. | Duplicate struct compositions can produce identity churn for handles and assertions that expect a single composition object. Proper fix: volatile/synchronized publication or reuse the parent composition map with an access key. Required stress: concurrent `Ref` struct/non-struct access-view creation with assertion checks. |
| `DONE` | `PropertyComposition` call-chain maps (`PropertyComposition.java:53`, `PropertyComposition.java:214`, `PropertyComposition.java:237`, `PropertyComposition.java:259`, `PropertyComposition.java:368`). | Owner is parent property composition/container; keys are method nested identity and property id. Invalidation is none. | `ConcurrentHashMap.computeIfAbsent`. Runtime reachable. | Map mechanics are safe. Keep owner-registration checks for foreign ids entering these paths. |
| `PROVEN CONFINED` | `ProxyComposition` and `CanonicalizedTypeComposition` mutable cache state (`ProxyComposition.java:39`, `ProxyComposition.java:59`, `CanonicalizedTypeComposition.java:44`, `CanonicalizedTypeComposition.java:74`, `CanonicalizedTypeComposition.java:113`). | No mutable cache in these classes. They delegate to owner compositions/templates. | Runtime reachable, but no local lazy state. | No cache fix needed beyond the generic `cloneAs` view-backing audit for handles returned through `ensureAccess`. |

## TypeConstant Caches

| Status | Cache/source | Owner, key, invalidation | Thread-safety and reachability | Failure mode, required proof, and proper fix |
| --- | --- | --- | --- | --- |
| `DONE` | `m_typeinfo` and invalidation count (`TypeConstant.java:1658`, `TypeConstant.java:1682`, `TypeConstant.java:1694`, `TypeConstant.java:1944`, `TypeConstant.java:8248`, `TypeConstant.java:8260`). | Owner is the `TypeConstant` constant pool; key is type identity. Invalidated by pool invalidation/adoption. | Volatile field plus `AtomicReferenceFieldUpdater`; slow path synchronized. Runtime/type metadata reachable. | Safe-publication mechanism is present. Keep invalidation-rank tests. |
| `MUST AUDIT` | Relation cache `m_mapRelations` plus recursion `TransientThreadLocal` (`TypeConstant.java:5906`, `TypeConstant.java:5919`, `TypeConstant.java:8015`, `TypeConstant.java:8018`, `TypeConstant.java:8267`, `TypeConstant.java:8272`). | Owner is the registered type's constant pool; key is left-hand `TypeConstant`. Cleared by `clearRelationMap()` and adoption reset. | Volatile map initialized as `ConcurrentHashMap`; recursion helper is atomically installed. Runtime reachable through type checks. | Thread mechanics are mostly sound, but key completeness needs proof for context-sensitive relation paths and auto-narrowing. Proper fix if context matters: key by relation context or avoid caching those cases. Required diagnostic: assert cached relation decisions are not made while an ambient context or auto-narrowing input affects the answer. |
| `MUST AUDIT` | `m_mapConsumes` / `m_mapProduces` (`TypeConstant.java:6531`, `TypeConstant.java:6534`, `TypeConstant.java:6577`, `TypeConstant.java:6580`, `TypeConstant.java:8030`, `TypeConstant.java:8038`, `TypeConstant.java:8279`, `TypeConstant.java:8284`). | Owner is the `TypeConstant` pool; key is formal type name, with access passed to the public method but not represented in the map key. Reset on adoption. | Plain lazy `HashMap`; source explicitly marks use as thread-unsafe. Runtime reachability must be proven or ruled out. | Concurrent recursion can leave `IN_PROGRESS`/`NO` decisions in the map; access-sensitive results may be incorrectly reused. Proper fix: prove compiler-only confinement, or replace with a concurrent/synchronized cache keyed by `(formal name, access)` and a per-thread recursion marker. Required stress: concurrent formal consumption/production checks with differing access values. |
| `DONE` | Former `TypeConstant.m_handle` cached `xRTType.TypeHandle` (`TypeConstant.java:7786`, `Container.java:116`, `Container.java:835`). | Owner is now the requesting `Container`; key is the registered `TypeConstant` in that container's pool. Invalidation is container lifetime. Foreign handles remain uncached. | `TypeConstant.ensureTypeHandle(Container)` normalizes shared foreign-pool types into the caller pool and delegates to `Container.ensureTypeHandle(...)`. The container cache uses `ConcurrentHashMap.computeIfAbsent(...)` and rejects type constants from another pool. Runtime reachable through every reflective Type path. | Fixed. The old cache was keyed only by `TypeConstant`, so two containers sharing one pool could reuse the first container's owner-bearing Type handle. The replacement preserves the intended one-handle-per-owner/type cache while adding the missing owner dimension and safe publication. `NativeTemplatesTest.typeHandlesAreCachedByContainerOwner()` fails on master because `TypeConstant.m_handle` exists there and passes here by verifying the cache moved to `Container`. |
| `MUST AUDIT` | `m_typeNormalized` (`TypeConstant.java:1216`, `TypeConstant.java:1221`, `TypeConstant.java:8299`). | Owner is the type constant pool; key is type identity. Reset on adoption. | Plain lazy immutable constant value. Runtime/type metadata reachable. | Likely duplicate-work only if constants are immutable and pool registration is stable, but publication is unguarded. Proper fix: volatile/atomic holder or precompute where type is published. Required stress: concurrent normalization during runtime type-info creation with late-registration diagnostics. |
| `MUST AUDIT` | `m_sJitName` (`TypeConstant.java:7143`, `TypeConstant.java:7146`, `TypeConstant.java:7152`, `TypeConstant.java:8294`). | Owner is JIT `TypeSystem`/module loader, but current key is only `TypeConstant`. Reset on adoption. | Plain lazy string. JIT/runtime reachability depends on JIT activation. | Wrong if multiple JIT type systems can assign different names to the same shared constant. Proper fix: prove single JIT owner per constant or key by `TypeSystem`/loader. Required proof: multi-module JIT name stability test. |
| `DONE` | Adoption reset for transient caches (`TypeConstant.java:7932`, `TypeConstant.java:7940`, `TypeConstant.java:7949`). | Owner changes from source pool to target pool. | `setContaining` clears transient caches after adoption. | Correct shape: owner-bearing helper cells do not cross pool adoption. Keep adoption regression tests. |

## TypeInfo, MethodInfo, PropertyInfo, And ChildInfo Caches

| Status | Cache/source | Owner, key, invalidation | Thread-safety and reachability | Failure mode, required proof, and proper fix |
| --- | --- | --- | --- | --- |
| `DONE` | `TypeInfoReal` owner-copy construction for `PropertyInfo`, `MethodInfo`, and `ChildInfo` (`TypeInfoReal.java:123`, `TypeInfoReal.java:125`, `TypeInfoReal.java:129`, `TypeInfoReal.java:265`, `TypeInfoReal.java:287`, `TypeInfoReal.java:310`, `TypeInfoReal.java:204`). | Owner is the containing `TypeInfoReal`; keys are property/method/child ids. Invalidation is type-info rebuild. | Constructor makes per-owner copies and validates ownership. Runtime metadata reachable. | This branch fixed cross-TypeInfo sharing. Keep validation enabled until the broader audit closes. |
| `DONE` | `TypeInfoReal` method lookup caches `f_cacheById` / `f_cacheByNid` and op lookup map publication (`TypeInfoReal.java:133`, `TypeInfoReal.java:1771`, `TypeInfoReal.java:1776`, `TypeInfoReal.java:2239`, `TypeInfoReal.java:2245`, `TypeInfoReal.java:2267`). | Owner is `TypeInfoReal`; keys are method identity/nested identity or operator lookup string. Invalidation is type-info rebuild. | Concurrent maps plus synchronization around bulk population. Runtime reachable. | Safe mechanics. Keep lookup cache stress. |
| `MUST AUDIT` | Plain derived `TypeInfoReal` maps and flags: `m_mapPropertiesByName`, `m_mapMethodsBySignature`, `m_delegates`, `m_fChildrenChecked` (`TypeInfoReal.java:576`, `TypeInfoReal.java:614`, `TypeInfoReal.java:824`, `TypeInfoReal.java:844`, `TypeInfoReal.java:1043`, `TypeInfoReal.java:1060`, `TypeInfoReal.java:1321`, `TypeInfoReal.java:1335`, `TypeInfoReal.java:2458`, `TypeInfoReal.java:2493`, `TypeInfoReal.java:2497`, `TypeInfoReal.java:2501`). | Owner is `TypeInfoReal`; keys are name/signature/delegates/newability state. Invalidation is type-info rebuild. | Plain lazy `HashMap`/object/boolean publication. Runtime metadata reachable. | Duplicate work, unsafe publication of mutable maps, and late pool growth during child newability checks. Proper fix: initialize immutable maps during construction, or volatile/synchronized `Map.copyOf` cells; make `m_fChildrenChecked` atomic/synchronized or precompute. Required stress: concurrent reflection/name lookups and `isNewable()` with virtual children under late-registration diagnostics. |
| `DONE` | `TypeInfoReal.m_mapMethodsByName`, `m_mapNestedProperties`, `m_setOps`, `m_setAuto`, `m_typeAuto/m_methodAuto` where accessed through synchronized methods (`TypeInfoReal.java:1128`, `TypeInfoReal.java:1159`, `TypeInfoReal.java:1750`, `TypeInfoReal.java:1899`, `TypeInfoReal.java:1936`, `TypeInfoReal.java:1967`, `TypeInfoReal.java:2021`, `TypeInfoReal.java:2041`). | Owner is `TypeInfoReal`; keys are nested property/method names or auto-conversion target. Invalidation is type-info rebuild. | Synchronized public paths guard creation and reads. Runtime metadata reachable. | Safe enough mechanically. Consider immutable map wrapping for defense, but not a priority. |
| `MUST AUDIT` | `MethodInfo.m_aBodyResolved` optimized method chain (`MethodInfo.java:102`, `MethodInfo.java:1158`, `MethodInfo.java:1187`, `MethodInfo.java:1258`, `MethodInfo.java:1299`, `MethodInfo.java:1301`, `MethodInfo.java:1672`). | Owner is the containing `TypeInfo`; key is the `MethodInfo` identity. Invalidation is type-info rebuild. | `forType` is synchronized and owner-copying, but optimized chain cache is a plain lazy array. The method body optimization can mark bodies native and attach generated delegation structures. Runtime call-chain construction reaches it. | Source claims idempotence, but unsafe publication and `MethodBody` mutation still need proof. Proper fix: synchronize/volatile-publish immutable optimized chains, or compute during `TypeInfoReal` construction after ownership copy. Required stress: parallel first call-chain construction for delegating/native/default methods with identity validation. |
| `MUST AUDIT` | `PropertyInfo.m_chainGet` / `m_chainSet` optimized accessor chains (`PropertyInfo.java:129`, `PropertyInfo.java:1225`, `PropertyInfo.java:1229`, `PropertyInfo.java:1233`, `PropertyInfo.java:1264`, `PropertyInfo.java:1268`, `PropertyInfo.java:1272`, `PropertyInfo.java:1639`, `PropertyInfo.java:1644`). | Owner is containing `TypeInfo`; current cache key is the `PropertyInfo` only, but the method accepts `idNested`. Invalidation is type-info rebuild. | `forType` is synchronized and owner-copying; accessor chains are plain lazy arrays. Runtime getter/setter call-chain construction reaches them. | If one `PropertyInfo` can be asked for multiple `idNested` values, the unkeyed cache is a correctness bug. Even if not, array publication is unsafe. Proper fix: prove single `idNested` per owned `PropertyInfo`, or key the cache by nested id and publish immutably/safely. Required tests: nested property getter/setter lookup through multiple access paths and parallel first construction. |
| `MUST AUDIT` | `PropertyInfo` helper cells `m_annotations`, `m_FInjected`, `m_FImplicitlyAssigned`, `m_typeBaseRef`, `m_idGetter`, `m_idSetter` (`PropertyInfo.java:1040`, `PropertyInfo.java:1076`, `PropertyInfo.java:1101`, `PropertyInfo.java:1110`, `PropertyInfo.java:1174`, `PropertyInfo.java:1182`, `PropertyInfo.java:1208`, `PropertyInfo.java:1211`, `PropertyInfo.java:1247`, `PropertyInfo.java:1250`, `PropertyInfo.java:1306`, `PropertyInfo.java:1309`, `PropertyInfo.java:1649`, `PropertyInfo.java:1674`). | Owner is containing `TypeInfo`/constant pool; keys are property identity and helper kind. Invalidation is type-info rebuild. | Plain lazy owner-pool constants, arrays, and booleans. Runtime metadata and field-layout creation reach these paths. | Duplicate work and unsafe publication; getter/setter/base-ref helpers can grow or pin owner-pool constants late. Proper fix: compute during owned `PropertyInfo` construction, or use synchronized/volatile immutable helpers with late-registration assertions. Required stress: concurrent field-layout and getter/setter chain construction with annotated refs. |
| `SHOULD FIX` | `ChildInfo.f_setIds` mutable set exposure (`ChildInfo.java:64`, `ChildInfo.java:69`, `ChildInfo.java:106`, `ChildInfo.java:216`). | Owner is containing `TypeInfo`; key is child name/identity set. Invalidation is type-info rebuild. | `forType` copies the set, but the final set is mutable and returned directly. Runtime metadata reachable. | Accidental mutation can corrupt child identity metadata. Proper fix: copy with `Set.copyOf`/unmodifiable set and return an immutable view. Required test: attempt mutation through `getAllIdentities()` and assert it fails or does not mutate owner state. |

## Op Address/Link Decision

The address/link group is not currently a `MUST FIX` for owner contamination because the inspected
fields store op references, offsets, exit counts, and scope/finally metadata, not `Frame` constants
or owner-bearing handles. It is also not `DONE`: the source shows eager resolution, but not safe
publication.

To close this as `DONE`, one of these proofs is required:

1. Eager-publication proof: every runtime-reachable `MethodStructure.Code` is constructed,
   `addressAndSimulateOps()` has run, and the linked op array is safely published before any `Frame`
   can obtain `function.getOps()`.
2. Owner-state fix: `MethodStructure.ensureCode()` safely publishes one fully linked code snapshot,
   e.g. synchronized/volatile/atomic `m_code`, with `Code` fields final or otherwise safely
   published.
3. Runtime diagnostic proof: instrumentation counters on `addressAndSimulateOps()` and
   `Op.resolveAddresses()` prove they never run after method/container runtime publication, and
   parallel first-execution stress cannot observe unresolved link fields.

If none of those proofs is accepted, the proper classification should be promoted from `MUST AUDIT`
to `MUST FIX` for the code publication cell, not for every individual op field.

## Same-Owner View Equivalence Proof

The cross-owner guard and same-owner view backing are both fixed and tested. The important invariant
is that `cloneAs(...)` is an access-view operation, not an object copy:

* Behavior: a source handle and each access-view clone share regular field storage; writes through
  either view are visible through the other.
* Behavior: inflated `RefHandle` values have per-view holder/backing state, so creating a struct view
  does not rewrite refs already visible through the revealed view, and vice versa.
* Behavior: refs obtained before masking/revealing continue to address the same logical property and
  do not silently switch holders when another view is created.
* Behavior: cross-owner masking still rejects non-shared handle graphs before `cloneAs(...)` is
  reached.
* Performance: the fix must not turn access masking into a deep copy of the full handle graph.
  Acceptable designs include a small shared backing object plus lazy per-view inflated-ref wrappers,
  or another representation with equivalent asymptotic behavior.
`GenericHandleCloneAsTest.sameOwnerCloneKeepsInflatedRefOuterViewLocal()` covers those semantics
directly using synthetic runtime handles. The test is intentionally written without branch-only
factory APIs so it can be copied onto master; on master it fails because the source ref's `$outer` is
rewritten to the clone. The fixed branch passes and also asserts that referent writes and regular
field writes still flow through the shared backing.

The chosen implementation is performance-equivalent to the old hot path:

* Handles with no access-view override still read and write the original final `m_aFields` array.
* Same-owner views still share regular field storage, so masking/revealing is not a deep copy of the
  full handle graph.
* Extra allocation is bounded by the number of inflated refs that actually need a different
  `$outer`, not by the total object graph size.
