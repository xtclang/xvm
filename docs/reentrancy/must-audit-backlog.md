# Must-Audit Backlog

This file tracks repository-wide state patterns that are not yet proven defects
but are too owner-sensitive or concurrency-sensitive to treat as ordinary style
cleanup.

`Must audit` sits between `must fix` and `should fix`:

- `Must fix`: a concrete race, wrong-owner cache, constructor escape, or
  broken lifecycle state is known.
- `Must audit`: the code may be safe only because of an assumption that is not
  encoded in the API, such as single-threaded compilation, per-container method
  graph ownership, no same-JVM reuse, or manual ThreadLocal cleanup.
- `Should fix`: the code is unnecessarily mutable or awkward, but current
  owner/lifecycle evidence does not make it a likely correctness bug.

The standard for closing a `must audit` item is higher than "it has not crashed
yet". Close it only by proving confinement/ownership, adding a focused test, or
moving the state behind an explicit owner/synchronization model.

## Master Control And Document Map

This file is the authoritative board. If another reentrancy document disagrees
with this one on status or priority, update this file first and treat the other
document as stale until reconciled.

Execution order:

1. **Close must-fix rows first.** Runtime/ASM rows that graduate from audit to
   concrete bugs require the full four-part discipline: prove red on old shape,
   fix, add/keep regression tests, and update the board.
2. **Finish the remaining must-audit umbrellas.** Audit by reading the actual
   sites. Do not classify by grep alone. Graduate only proven defects.
3. **Prepare/file Category A master bugs.** Use
   `plans/master-issue-submissions.md` for the 18 master bug issue bodies,
   patch material (exact master-form hunks where extracted, labeled sketches
   elsewhere - the readiness matrix says which is which per row), and
   deterministic red proofs.
4. **Prepare broader issue/PR tracks.** Use
   `plans/global-issue-pr-backlog.md` for diagnostic authority, nullness,
   duplication/control-flow, logging, world X-ray, and LSP/compiler ownership
   rows that are not all single master bugs.
5. **Launch reviewable PRs.** Use `plans/github-issue-breakdown.md` for the
   implementation PR sequence and per-PR reviewer checklist. Treat this branch
   as an integration/proof branch, not the final review unit.
6. **Keep the presentation synchronized.** Use `presentation.md` for the
   meeting narrative only. It should link to evidence, not become the board.

Document roles:

| Role | File | Update when |
| --- | --- | --- |
| Authoritative board | `must-audit-backlog.md` | Any status, priority, open/closed, or must/should/audit classification changes. |
| Master bug filings | `plans/master-issue-submissions.md` | A Category A issue body, exact master diff, red proof, or readiness note changes. |
| Broader issue/PR backlog | `plans/global-issue-pr-backlog.md` | A non-Category-A issue or PR-sized track is added, split, reprioritized, or made ready. |
| PR launch plan | `plans/github-issue-breakdown.md` | A branch PR sequence, dependency, cherry-pick result, or reviewer checklist changes. |
| Proof/evidence ledger | `test-failure-evidence.md`, `runtime-ownership-hardening-ledger.md`, `fixed-in-this-branch.md` | A test proves red/green behavior, a fix lands, or evidence changes. |
| Topic audits | `*-audit.md`, `plans/*.md` | The underlying site census, analysis, or design detail changes. |
| Presentation | `presentation.md` | The sales narrative, click-through demo, or teleprompter script changes. |

## Current Wide Scans

These commands were run on branch `lagergren/lazy-instance` on 2026-08-21.

Manual lazy null caches across all Java sources:

```bash
rg -U --pcre2 -c \
  "if\s*\(\s*((?:this\.)?(?:m_|s_|f_)[A-Za-z][A-Za-z0-9_]*)\s*==\s*null\s*\)\s*\{[\s\S]{0,320}\1\s*=(?!=)" \
  javatools/src/main/java | awk -F: '{s+=$2} END {print s+0}'
```

```text
40 strong same-field lazy-init matches across javatools/src/main/java
20 of those are in runtime/asm
```

Non-final static fields across all Java sources:

```bash
rg -n --pcre2 \
  "^\s*(public|protected|private)?\s*static\s+(?!final\b)(?!class\b|interface\b|enum\b)[^;(=]+\s+[A-Za-z_][A-Za-z0-9_]*\s*(?:=|;)" \
  javatools/src/main/java | sort
```

```text
javatools/src/main/java/org/xvm/compiler/ast/ConditionalStatement.java:80
javatools/src/main/java/org/xvm/compiler/ast/ElseExpression.java:220
javatools/src/main/java/org/xvm/compiler/ast/ElvisExpression.java:316
javatools/src/main/java/org/xvm/compiler/ast/MethodDeclarationStatement.java:1265
```

Re-run 2026-08-24: the four remaining hits are exactly the compiler label/lambda
counters delegated to the separate `lagergren/compiler-counter-atomics` branch.
The other four originals (`Ctx.MD_inject` and the three JIT builder name
strings) were immutable values missing `final` and are now `static final`.

Ambient and weak/identity state scan:

```bash
rg -n "ThreadLocal|TransientThreadLocal|ScopedValue|WeakHashMap|IdentityHashMap|volatile|synchronized\s*\(" \
  javatools/src/main/java/org/xvm
```

This scan is intentionally broad. Each hit needs classification by owner and
lifetime, not blind conversion.

## Consolidated Must-Fix Task List

This list intentionally pulls from every reentrancy document in this folder:
runtime ownership, `this-escape`, lint, constant-pool/adoption, clone usage,
manual lazy caches, JIT implications, and same-JVM stress planning. If a
category below is marked `must audit`, it becomes `must fix` as soon as a test,
diagnostic, or code inspection proves owner sharing, cross-request reuse, or
runtime publication.

### 2026-08-28 sweep (post rebase-onto-master + display-purity campaign)

Each claim below was re-verified against the tree, not carried over from the previous
sweep. **The 2026-08-26 counts underneath this section are stale where they conflict.**

- **MUST-AUDIT: 0 open.** MA1-MA5 all remain closed as audits; residues are parked to
  the JIT rows (J21-J24), not open audits.
- **MUST-FIX: 7 open, unchanged** — MF1 (injector deadlock, filed as master issue
  #541, needs a wait-graph design, not a hunk); MF2-MF6 (the JIT cluster J21-J24,
  unfiled AND unfixed, parked for the JIT rebase); MF7 (ConstantPool transactional /
  freeze-on-publish rewrite = enhancement-list **E3 Part B**). None is a small hunk.
- **SHOULD-FIX closed since the last sweep** (verified in-tree today):
  - **Side-effect-free `toString`/display path — DONE.** This was parked "near the end
    of the should queue"; the whole campaign landed, including the two enforcement
    gates. `DisplayPurityTest` runs with an EMPTY baseline and
    `DisplayPurityRuntimeTest` proves it empirically (pool does not grow while
    rendering 100+ live objects; carries a negative control). See
    `plans/side-effect-free-tostring.md`.
  - **MA5's 8 leaky getters — DONE (8/8 verified fenced).** Includes the two that a
    naive grep reports as live: `ModuleStructure.collectModuleDependencies` wraps at
    its `return`, and `ConstantPool.getJitPrimitiveTypes` caches an already-
    `unmodifiableSet` value.
  - **MA4's 3 ambient `getCurrentContext()` readers — effectively closed.**
    `Argument`/`OpVar` no longer read ambient context at all (they take an explicit
    `Frame` in the forced overload); `Utils.log` no longer NPEs on a null ambient.
  - **MA2's `markNative()` residual — half done.** `m_fNative` is now `volatile`
    (`m_code` already was), closing the visibility pair.
  - **DirRepository scan-cache race — DONE and PROVEN**, filed as master bug row 27
    (red on master `82683bcd2` with `ConcurrentModificationException`, green here).
  - **Array Stage 3 `ListMap`-getter remnant — appears closed**: no public
    `ListMap`-typed getter remains on `TypeInfo*`/`ClassStructure` (the two surviving
    `public ListMap<...>` returns are `MethodStructure.resolveTypeParameters` and
    `ModuleInfo.classNodes`, neither of which is the flagged metadata-getter shape).
- **SHOULD-FIX still open (actionable, small) — re-verified in-tree 2026-08-28:**
  - ~~`markNative()`'s other half~~ **DONE 2026-08-28**, and the board's prescription
    was WRONG as written. "Refuse the transition once code exists" would reject the
    legitimate use: replacing an Ecstasy body with a native one is what `markNative` is
    FOR (`xConst` marks `equals`/`compare`/`hashCode` native, and those have code). The
    hazard is not that code exists, it is that READERS exist - so the guard tests runtime
    publication, exactly as `forceAssembly` does, and lives at the head of `markNative()`.
    Verified not to fire during a real boot (`xdk:installDist` green) and gated.
    Master-portability note recorded on bug row 28: the guard needs E3's publication
    marker, which master does not have, so master's portable fix stays the volatiles.
  - Family C array deferral (tracked in the array-exposure docs).
  - **CORRECTION:** `Utils.log`'s real closure is **DONE**, not open. `Utils.log` now
    takes an explicit `ServiceContext`, the `Frame` overload derives it, and
    `ServiceContext.getCurrentContext()` was removed outright once it had no callers -
    the only surviving mention is a historical comment. MA4 is fully closed.
- **CLOSED by verification, not by work:**
  - **Arrow-switch remnant — verified, effectively closed.** Rather than trust the
    by-policy claim, all 55 `@SuppressWarnings("fallthrough")` were removed and the
    FATAL fallthrough lint was allowed to identify the real ones (iterating, because
    the compiler aborts on the first fatal error - an early "38 are stale" reading was
    an artifact of that). Result: **54 are genuine cascades, 1 was stale**
    (`NamedTypeExpression`, removed). None of the 54 switches on a sealed hierarchy:
    they are enums (`Composition` x9, `Token.Id` x5, `Format` x3, `Relation`),
    state-machine stages/counters (`stageNext`, `m_stage`, `nState`, `ixStep++`),
    chars (Lexer), Strings and ints. Sealing fixes CLASS dispatch; these are protocol
    machines over scalars where the fallthrough is the intended semantics.
  - **Sealed `Op` split — measured and RECOMMENDED AGAINST** (see the 2026-08-28
    section in `sealed-hierarchy-audit.md`). `Op` is dispatched by OPCODE, not class:
    824 opcode sites versus 14 `instanceof` checks, execution via virtual
    `op.process(...)`, construction via a `switch (nOp)` factory. Direction A (move the
    15 direct subclasses up) is cheap but yields no exhaustiveness; Direction B (one
    package) needs a **210-entry permits clause** and import rewrites in 177 + 196
    files. The hazard sealing removes - a silently-unhandled subtype - does not exist
    here, because an unknown opcode already fails loudly in the factory. Decided, not
    deferred.
- **New master bug filed today: row 28** — `MethodStructure` native/code state
  published with no happens-before edge. Master is worse than this branch was: BOTH
  `m_fNative` and `m_code` are non-volatile there AND `ensureCode()` lazy-inits
  unsynchronized. Filed on JMM/inspection evidence (like rows 19/26) with an honest
  no-deterministic-red statement.
- **Documentation defect fixed:** the rebase rewrote all 297 branch commits, so every
  hash cited in `plans/master-issue-submissions.md` and
  `plans/master-enhancement-submissions.md` was dead. Both now carry a hash→subject
  resolution appendix (subjects survive rebases) and a warning; all cited hashes are
  covered and the branch subjects verified to resolve.

### 2026-08-28 sweep B (clone retirement + stage-4 primitive escapes)

Later the same day. Everything below was verified in-tree, not carried over.

**CLOSED this wave:**

- **Clone idiom retired repo-wide — DONE.** All 63 array `.clone()` sites in main
  sources now route through `Handy.copyOf` (generic + byte/char/int/long overloads).
  `Object.clone()` survives in exactly ONE place: `ObjectHandle.cloneAs`, the
  documented island. The helper exists rather than inlining `Arrays.copyOf(x, x.length)`
  for a load-bearing reason: it takes the array as a PARAMETER, so the argument is
  evaluated once — inlining would double-call a method receiver (`getChain().clone()`)
  and double-read a volatile field, where two reads can yield different arrays and
  mis-size the copy. It is also the single line to edit if the mechanism must change back.
- **Stage-4 primitive raw-array escapes — DONE (9/9).** `FrozenArray<T>` is generic and
  cannot hold primitives, so nine escapes were unclosable AND invisible to the ratchet.
  Added `FrozenByteArray`/`FrozenCharArray`/`FrozenIntArray` and closed all nine. Three
  were pool-interned payloads whose hash is cached over their contents, so a consumer
  writing through the alias could silently invalidate hash/equality for every holder.
  `xString` handed out the `char[]` backing an Ecstasy String — and the crypto templates
  passed that same array to Java `KeyStore` APIs, which may retain or zero it. See
  `array-element-exposure-audit.md` (Stage 4 Candidate Survey).
- **Escape ratchet raised 115 → 134, the ONE sanctioned raise**, with per-site
  justification recorded in `FrozenArrayEscapeRatchetTest`'s javadoc. Read the increase
  correctly: the metric became more honest, not the exposure worse. A raw `byte[]` getter
  was an unbounded UNCOUNTED escape to every consumer; it is now a frozen payload with a
  few counted zero-copy reads inside the class that owns it. Net exposure fell.
- **New master bug: row 30 — `Version.isSameAs()` indexes the wrong array.** Found while
  surveying `Version.getIntArray()` as a `FrozenIntArray` candidate. The remainder loop
  selects the longer array into `remaining`, iterates to ITS length, then indexes
  `thatInts` anyway. `"1.2.3".isSameAs("1.2")` throws `ArrayIndexOutOfBoundsException`
  instead of returning false, and `"1.2.0".isSameAs("1.2")` throws instead of returning
  true — so the trailing-zeros semantic the method exists to provide never works in that
  direction. **FILED as PR #550** (one word + regression test), verified red on master.

**NEW MUST-AUDIT: MA6 — `ObjectHandle` per-view field classification.**

Retiring the last `Object.clone()` is NOT a copy-constructor job; see
`objecthandle-clone-island-resolution.md` for why that would make things worse across a
49-subclass hierarchy whose copy must be exhaustive. The real defect is that a shallow
copy of a VIEW splits per-view state from shared storage — the freeze-split family.

The audit: enumerate every field across the 49 `ObjectHandle` subclasses and classify each
as (a) view-identity (`m_clazz`, overwritten by `cloneAs`), (b) deliberately shared
reference, (c) immutable value, or (d) **per-view mutable state**. Category (d) is the bug
list; each entry moves into a shared cell following the `FreezeCell`/`ArrayState` precedent.

This becomes MUST-FIX for any category (d) field found, per this list's own rule. When (d)
is empty the shallow copy is provably harmless, the island is proof-defended rather than
convention-defended, and `supportsMutableViews()` default-deny becomes retirable with
evidence. Two known starting entries, both needing verification rather than assumption:
`GenericHandle.m_aFieldOverrides` (cloneAs copies the reference, so views share one override
array — whether writes through one view should be visible through the other is unestablished)
and `m_owner`.

**NEW SHOULD-FIX: SF-Lazy — convert one-shot lazy-init fields to `final Lazy`, SCOPED.**

Census: 49 `if (field == null) { … field = … } return field;` sites across 27 files. The
naive reading — "these could all be `final Lazy`" — is wrong, and the disqualifiers are
hard, not stylistic:

1. **Reset/invalidated fields (8 sites): impossible.** A `final Lazy` cannot be cleared.
   `Scope.m_scopeChild`, `LambdaExpression.m_lambda`, the `m_listContinues` family.
   `ModuleStructure.m_abDigest` and `MethodStructure.resetRuntimeInfo` are the same shape.
2. **Externally supplied values: does not fit.** `NativeContainer.m_hRootDir` is assigned
   from a different method with a caller-supplied value; `Lazy` needs a self-contained
   `Supplier`.
3. **Not actually caches.** `Frame.m_continuation` is plain mutable state — a census false
   positive. The 34 "never reset" figure is an upper bound and needs per-site reading.
4. **Instance count is the deciding axis.** `Lazy` costs an object plus an internal
   `AtomicReference` per field per instance. Fine on a `Container` (one per runtime); a
   regression on `Op`, `Frame`, `Parameter`, or any handle. The tree already records this
   judgement in `xString`: *"benign race can only compute the same value twice, while Lazy
   would add two objects to every StringHandle."* Honour that precedent.
5. **Reentrancy.** `Lazy.of` synchronizes, and type resolution is recursive (`MethodInfo`'s
   own "turtles" comment). Recursive computes need `ofUnsynchronized`, or they risk
   deadlock.

**Therefore the scope is:** long-lived, low-cardinality owners only — containers, templates,
module/file structures, pool-level caches — where the field is computed once from a
self-contained supplier and never reset. Explicitly OUT of scope: anything on `Op`, `Frame`,
`Parameter`, `ObjectHandle` subclasses, or per-`Constant` instances. Land incrementally, one
owner class per slice, each with a test pinning compute-once. Not a sweep.

### 2026-08-26 reconciliation (post embedding-API)

Backlog swept after the embedding-API wave shipped. Net state:

- **CLOSED this wave:** the embedding-API must-fixes — ledger rows 66
  (`MainContainer.futureResult`, also closes issue-543 items 1a/1b), 67
  (`Container.runModule` + `NestedContainer.createForHost` nested-host linchpin),
  68 (`org.xvm.api.XtcEngine` in-memory compile + run + `writeTo`). Both gates
  green; `XtcEngineTest` (4) run, skipped=0.
- **NEW forward work added below the line** (not previously on any list):
  - **FW1 — Master contrast branch** (demonstration deliverable): reproduce the
    compile/run API on a fresh branch off `master` per
    `instructions-port-run-compile-api-to-master.md`. Compile half ports free;
    run half = ~130 lines of hooks minus `markRuntimePublished`. Proves master is
    fine for sequential one-shot but exposed to the 543 pool-race subset under
    warm/parallel reuse. Priority: user call (evidence piece, not hardening).
  - **FW2 — Gradle plugin warm-engine adoption** (large perf win): drive one warm
    `XtcEngine` from the existing `DirectRuntimeBuildService` instead of a
    per-invocation launcher. Full phased task list + single-flag old/new-world
    toggle in `gradle-plugin-uses-run-compile-api.md` §8-9. Phase 1 (warm runs) is
    low-risk/no-XDK-change; Phase 2 (warm compile) blocked on FW3.
  - **FW3 — `XtcEngine.compile(Path...)` source-tree entry point** (prerequisite):
    compile on-disk module directory trees via `ModuleInfo`, not just in-memory
    strings. Unblocks warm plugin compile (FW2 Phase 2) AND the LSP source-tree
    case. `XtcEngine.java:123-128` TODO.
- **Reclassifications (stale corrections):**
  - Array Stage 3 (this section's array-exposure row, ~line 205): Families A/B +
    FrozenArray are DONE (ledger rows 60-64), Family C deferred by analysis. Only
    the three `ListMap`-typed getters (retype to a `TypeInfo` interface + ~10-site
    caller sweep) genuinely remain — treat as SHOULD-FIX remnant, not open Stage 3.
  - "Retire `Object.clone()` in structure family" row (~line 198) is marked Done
    but its Why/closure text still reads as an unexecuted plan — **needs a human to
    confirm the copy-constructor + `Cloneable`-removal actually landed vs a
    mislabel** before trusting the Done.
- **Open counts after sweep:** MUST-FIX 7 (MF1 injector deadlock #541; MF2-MF6 the
  JIT cluster J21-J24, unfiled + parked for JIT rebase; MF7 ConstantPool
  transactional rewrite). MUST-AUDIT 4 umbrellas (3 deferred to JIT/compiler
  branches). SHOULD-FIX ~24 (structural / sealing / diagnostics-API / small
  follow-ups, many mapping to the 17 Global-backlog tracks; none filed to master).
- **Master filing:** only Issue #541 and PR #539 have numbers; all 18 Category-A
  drafts + rows 19/20/25/26 are fixed-in-branch but UNFILED; J21-J24 unfiled AND
  unfixed.
- **MUST-AUDIT CLOSURE 2026-08-26 — all four umbrellas audited + documented:**
  - **MA1** (owner-bearing metadata caches): remnant = the `m_sJitName` caches -
    proven owner-bearing (per-`Xvm` unique suffix cached against a shared constant)
    but with ZERO interpreter reach (all callers are JIT codegen). Verdict:
    must-audit-today / must-fix-before-JIT-concurrency, **parked to the JIT rows**
    (J21-J24, `jit-global-owner-classification.md`). Doc:
    `runtime-metadata-op-cache-classification.md`.
  - **MA2** (frozen-code writer diagnostic): `forceAssembly` diagnostic already
    DONE; residual = **`markNative()`** non-atomic 3-step transition with a plain
    (non-volatile) `m_fNative` - a racing `getOps()->ensureCode()` can observe
    `native=false, code=null` and die. Verdict: **NEW SHOULD-FIX, concurrency-only,
    ACTIONABLE-NOW** (volatile `m_fNative` + a `forceAssembly`-shaped throw at the
    head of `markNative()`). Doc: `runtime-metadata-op-cache-classification.md`.
  - **MA3** (compiler AST/context mutation): 6 distinct compiler-mutation surfaces
    (`Context` maps, `NameResolver`, `InvocationExpression`/`LambdaExpression`
    caches, break/continue lists, label/`CaseManager` scratch) inventoried and
    classified - **all compiler-branch-deferred, record-only, no runtime/ASM
    must-fix graduated.** NEW doc: `compiler-ast-context-mutation-audit.md`.
  - **MA4** (remaining ambient explicit-owner conversions): one runtime slice open -
    the 3 `ServiceContext.getCurrentContext()` diagnostic readers (`Argument.java:51`,
    `OpVar.java:115`, `Utils.java:473`), **NEW SHOULD-FIX / ALWAYS** (diagnostics-only,
    but `Utils.log` NPEs on a null ambient); closure = pass `Frame`/`ServiceContext`
    explicitly. The 5 static `Ctx.get()` captures are parked to the JIT rows. STALE
    CORRECTION: the "compiler-side `withPool` scopes" remnant no longer exists
    (`withPool`/`getCurrentPool`/`setCurrentPool` have zero call sites) - already done
    per board row 252; the row-270 wording is stale. Doc:
    `ambient-registry-must-audit-classification.md`.
  - **Net:** the four must-audits are CLOSED as audits. Two NEW actionable SHOULD-FIX
    items surfaced (both concurrency/diagnostics, safe to fix on this branch):
    `markNative()` writer diagnostic + volatile flag, and the ServiceContext ambient
    diagnostic readers. The remaining must-fix residue (MA1 JIT-name, MA4 static Ctx)
    is genuinely parked to the JIT rebase. No must-audit remains un-done.
- **MA5: mutable-escape / aliasing audit — DONE 2026-08-26** (`mutable-escape-audit.md`).
  Result: the runtime/asm mutable surface is **overwhelmingly fenced** (unmodifiable
  wrappers, `ROMap`/`List.of`, `emptyX`, defensive `aOwned` copies, copy-in setters).
  **MUST-FIX: 0** (no caller mutates a returned collection to corrupt an owner).
  **SHOULD-FIX: 8** leaky getters returning a live field directly - runtime
  `ServiceContext.getFibers`/`Container.getServices`/`Fiber.getTokens`,
  `getFieldLayout` record-map (verify), asm `Component.getChildByNameMap` /
  `ModuleStructure.getDependencies` / `ConstantPool.getJitPrimitiveTypes` (JIT) /
  `MethodStructure.getReturns`/`getParams` (`Arrays.asList` set-through). All
  aliasing-latent or concurrency-only; cheap batch fix (return unmodifiable views),
  low priority. Shapes (b) stored-alias and (c) shared-static-mutable are CLEAN.
  `ErrorList.getErrors` = safe-by-convention (request-local diagnostics).
  Original scope (kept for reference):
  A systematic sweep of every MUTABLE value that (a) is RETURNED from a method
  (getter/factory leaking internal mutable state a caller can mutate), (b) is
  PASSED INTO a constructor/setter and STORED DIRECTLY (caller keeps a live
  alias), or (c) otherwise ends up in a FIELD from an externally-owned source -
  across `runtime/` and `asm/` (compiler noted, deferred). Each site classified:
  MISUSE reachable today (must-fix) / broken-only-under-concurrency / safe-by-
  convention or already-immutable / already-covered (cross-ref
  `array-element-exposure-audit.md`, `array-list-immutability-study.md`,
  `clone-usage-audit.md`, `constant-adoption-clone-audit.md`, `generics-api-audit.md`
  so this does not re-audit the array/clone/adoption work). Goal: determine where
  XVM already misuses shared mutable references vs where the frozen/immutable work
  already covers it. Deliverable: `mutable-escape-audit.md` (new) or augment the
  closest existing home. User-requested 2026-08-26.

### 2026-08-26 ToolConnector hazard-analysis feedback

The `ToolConnector` (née LspSupport) desired-API analysis
(`toolconnector-api-proposal.md` §4, MASTER-based) enumerated what breaks when a
long-running host runs consecutive/parallel nested containers and compiles in
container 0. Most items map to existing rows (543, MA1-MA5, JIT J21-J24). NEW /
sharpened backlog items and verified closures:

- **SHOULD-FIX — shared-pool eviction / long-running memory bound (MEASURED: slow
  leak, not imminent).** The shared native/library `ConstantPool` interns per-run
  types + `TypeInfo` and never evicts on container disposal (543 §1c).
  `SharedPoolGrowthCharacterizationTest` measured it: after a one-time ~2000-constant
  first-run warmup, each DISTINCT-typed run adds only **~1-2 constants permanently** -
  monotone and unbounded in principle, but a SLOW leak, so the urgency is "eventual for
  a very-long-lived diverse-workload host," not the imminent-OOM framing used earlier.
  Still the #1 long-running-host correctness issue (unbounded is unbounded). This is the
  existing frozen-base+runtime-annex should-fix, but it GRADUATES to a hard blocker
  for any resident tool (LSP / daemon / `ToolConnector`), independent of
  concurrency - the #1 long-running-host issue. Closure = scope-bound / evictable
  annex tied to container/run lifetime. **DESIGN/PLAN written 2026-08-26:
  `constant-pool-freeze-annex-design.md`** - the frozen-base + runtime-annex
  architecture that closes BOTH this leak (#2) and #10 (upstream/child-pool
  concurrent reads) with one fix, in phases A-D each gated. This is the real fix
  that `markRuntimePublished` retrofits; do NOT extend the marker to more pools.
- **MUST-AUDIT — concurrent-compile shared-ASM mutation (container-0 compiles) —
  DONE 2026-08-26** (`compiler-ast-context-mutation-audit.md`, "Concurrent-Compile
  Shared State" section). Finding: for concurrent DIFFERENT-module compiles (the
  common case) the compile pipeline is per-compile-ISOLATED - the read-through
  `LinkedRepository` clones each library into the compile's own `BuildRepository`
  and link points the fingerprint at that clone, so `registerConstants`/merge/
  NakedRef/interning are per-compile; `ErrorList` is per-request; and the MA3
  AST/`Context` surfaces are per-compile (they bite INCREMENTAL/same-module, not
  parallel-different). The two REAL concurrent-compile blockers, both **MUST-FIX for
  concurrent compiles**: (1) **`DirRepository` lazy materialization is
  unsynchronized** - `ensureModule()`/`ensureCache()` + plain `TreeMap`/`HashMap`
  race on first concurrent load of a library module. **FIXED 2026-08-26 (ledger row
  69):** coarse per-repository synchronization of `loadModule`/`getModuleNames`/
  `storeModule` (correct granularity for a lookup cache). Residual: the returned
  module is still lazy, so concurrent CLONE (read-through `LinkedRepository`) relies
  on prewarm (`prelinkSystemLibraries` covers system libs); fully-materialize-on-load
  is a follow-up for lazy user libraries. (2) **static compiler counters**
  (`s_nLabelCounter` etc., still parked to `lagergren/compiler-counter-atomics`). SHOULD-FIX/verify: the clone READS the
  shared library instance, so prewarm is load-bearing (a lazily-materialized library
  child touched during clone-merge would race). No NEW runtime/ASM must-fix
  graduated; interim stance = SERIALIZE compiles until (1)+(2) land.
- **Diagnostics/logging request-scoping** stays the existing should-fix
  (request-owned diagnostic authority; MA4 ambient `getCurrentContext` readers;
  `BLACKHOLE`; shared `ErrorList`) - see `logging-diagnostics-audit.md` and Global
  backlog #1-3. `ToolConnector` needs per-request correlated sinks; no new row.

- **VERIFIED-SAFE (checked against MASTER 2026-08-26; do NOT re-audit):**
  - `TypeInfo` cache poisoning on failure - NOT a hazard: `m_typeinfo` `volatile`
    (`:8233`), `setTypeInfo` monotonic CAS so a partial never overwrites a complete
    (`:1946`), `isComplete` excludes placeholder/incomplete (`:2033`) so a failed
    build's residue triggers a REBUILD, serious errors `invalidateTypeInfo()`
    (`:1821`); `f_mapRefTypes` is `computeIfAbsent` (nothing cached on throw).
  - Compositions per-container (`f_mapCompositions` per-`Container`
    `ConcurrentHashMap`, master `Container.java:745`) - no cross-container residue
    in the nested model (the `relocateConst` residue was the sibling-main path).
  - Container/service registries - `Runtime.f_containers` a synchronized
    `WeakHashMap` (disposed containers auto-evict), `f_setServices`/`f_setFibers`
    concurrent - lifecycle churn is safe; MA5 leaky getters are aliasing-only.

| Status | Task | Source category | Why it matters | Required closure |
| --- | --- | --- | --- | --- |
| Done in this branch | Remove constructor-published native template `INSTANCE` globals and static owner metadata | `must-fix-races.md`, `state-inventory.md`, `fixed-in-this-branch.md` | Process-global mutable template/metadata state was the original last-writer-wins, wrong-container, constructor-publication bug family. | Keep `NativeTemplates`, owner-local lazy metadata, and source scans that prevent `INSTANCE = this` from returning. |
| Done in this branch | Replace split `SingletonConstant` lifecycle state | `must-fix-races.md`, `fixed-in-this-branch.md` | Separate handle/fiber/future fields allowed impossible mixed initialization snapshots. | Keep the `AtomicReference<InitState>` state machine and singleton lifecycle tests. |
| Done in this branch | Fix adopted constant runtime/helper state leaks proven by stress | `constant-adoption-clone-audit.md`, `stress-discovered-runtime-issues.md` | Shallow clone/adoption copied owner-local runtime state across pools, including the proven singleton handle leak. | Keep the final adoption wrapper, explicit `copyForAdoption(...)` hooks, and `ConstantAdoptionValidator` stress runs. |
| Done in this branch | Close public/native natural enum raw-handle publication paths | `must-fix-races.md`, `native-template-startup-safety.md` | Public paths could expose natural enum construction structs instead of initialized enum singleton handles. | Keep public enum helpers routed through initialized/deferred enum handles and owner-local native enum factories. |
| Done in this branch | Move `NativeContainer` startup work out of the constructor | `this-escape-removal-audit.md`, `fixed-in-this-branch.md` | Startup installed owner-local templates/resources while the owner object was still under construction. | Keep `NativeContainer.create(...)` and lint tests that show no `NativeContainer.java` `this-escape` warnings. |
| Done in this branch | Remove runtime handle constructor publication/mutation escapes | `this-escape-removal-audit.md`, `fixed-in-this-branch.md` | `RefHandle` and file-node handles wrote themselves or backing fields through visible paths before construction completed. | Keep factory construction and `RefHandleConstructionTest`. |
| Done in this branch | Remove runtime constructor assertion escapes and `ClassTemplate` implicit-field hook | `this-escape-removal-audit.md`, `fixed-in-this-branch.md` | Constructor-time virtual calls made partially constructed runtime objects observable to subclass behavior. | Keep static/private constructor validation and explicit `ClassTemplate` metadata. |
| Done in this branch | Remove ASM `Op` constructor-shape virtual dispatch | `this-escape-removal-audit.md`, `fixed-in-this-branch.md` | Base opcode constructors called subclass-overridable shape predicates while deserializing packed operands before subclass construction completed. | Keep constructor-only shape metadata, private `ConstantRegistry` helpers, and `OpRuntimeCacheTest` layout/footprint guards. |
| Done in this branch | Remove utility constructor helper dispatch | `this-escape-removal-audit.md`, `fixed-in-this-branch.md` | `PackedInteger`, `HasherReference`, and `ListSet` constructors called public/protected APIs while subclass construction was incomplete. | Keep private constructor helpers and `UtilityConstructorEscapeTest`. |
| Done in this branch | Remove `MethodInfo`/`PropertyInfo` body owner constructor escapes | `this-escape-removal-audit.md`, `fixed-in-this-branch.md` | Method/property owners passed partially constructed `this` into `MethodBody.forMethod(...)` and `PropertyBody.forProperty(...)`, so child metadata could observe default owner state. | Keep static factories, non-virtual owned body copies, final body arrays, focused constructor-escape tests, and `TypeInfoReal` ownership validation. |
| Done in this branch | Remove ASM metadata owner-copy constructor escapes | `this-escape-removal-audit.md`, `fixed-in-this-branch.md`, `ownership-diagnostics.md` | `FileStructure`, `MethodStructure`, `PropertyStructure`, `VersionTree`, `PropertyConstant`, and `TypeInfoReal` used constructor-time virtual hooks or mutable source metadata owner assignment. Parallel `TypeInfoReal` construction could let one owner steal shared unowned method/property/child metadata. | Keep final root/owner assembler types, static/private constructor validation, per-owner `forType(...)` copies, focused constructor-equivalence tests, and parallel `TypeInfoReal` ownership tests. |
| Done in this branch | Remove `ModuleInfo` resource-dir constructor escape | `this-escape-removal-audit.md`, `fixed-in-this-branch.md` | The explicit-resource constructor path called overridable `getResourceDir()` before source/binary/resource fields were fully assembled. | Keep private `ensureResourceDir()` for constructor-time cache initialization and `ModuleInfoTest` hook coverage. |
| Done in this branch | Remove runtime-executed owner-bearing `Op` caches | `manual-lazy-cache-audit.md`, `fixed-in-this-branch.md`, `runtime-metadata-op-cache-classification.md` | Shared decoded op graphs could cache `Frame` constants, runtime operands, or owner-bearing switch tables from one owner and reuse them in another. | Keep frame-local resolution for condition/common-type constants, per-execution `JumpNSample` interval handling, container-owned runtime-op switch caches for `JumpVal`/`JumpVal_N`, and `OpRuntimeCacheTest`. |
| Done in this branch | `JumpNFirst.m_fVisited` runtime state on decoded op | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md` | `assert:once` is deliberately keyed by the decoded op, but the old plain boolean made concurrent first execution a data race. | Keep the final `AtomicBoolean` and `OpRuntimeCacheTest` sequential/concurrent once-state tests. |
| Done in this branch | Fix first-PR manual lazy null cache hazards | `manual-lazy-cache-audit.md`, `fixed-in-this-branch.md` | Regex, FS path, and native file-node creation caches had real publication or owner-transfer hazards. | Keep final `Lazy` where safe, adoption cache clearing, and file owner validation. |
| Done in this branch | Fix weak registry monitor mismatch | `runtime-ownership-hardening-ledger.md` | `Runtime.findContainer(...)` could iterate a weak registry while another path registered/expunged containers under a different monitor. | Keep common monitor access and `RuntimeTest`. |
| Done in this branch | Fix structural hash lint contracts and make `Constant.m_iHash` publication explicit | `lint-parallelism-risk-audit.md`, `fixed-in-this-branch.md` | `equals(...)` without matching `hashCode()` corrupts ordinary maps/sets; a plain cached constant hash field was an unexplained race-shaped cache. | Keep recomputed hashes for mutable metadata, volatile resolved-only constant hash cache, and hash contract tests. |
| Done in this branch (cherry-picked from PR #539) | Utility constructor escape hazards | `this-escape-removal-audit.md` | `CooperativelyCleanableReference` published `this` to a static set; `AbstractConverterMap` called overridable factories from the base constructor. | The PR #539 commits (including the reviewer-requested lazy private-field view caching) are cherry-picked into this branch; their tests run in the composite build. Follow-up recorded in the PR 6 slice of `plans/github-issue-breakdown.md`: convert the converter-map view caching to `Lazy.ofOwner(...)`, retiring the one documented benign-race special case. PR #539 now carries the `Lazy.Owner`/`ofOwner` API itself as an additive backport while keeping the plain fields during review, so the conversion becomes its own tiny diff once that review settles. |
| Split/background | Compiler counters | `state-inventory.md`, previous compiler counter branch | Process-global non-atomic counters are wrong for parallel/same-JVM compiler requests. | Keep this out of the runtime branch; finish the separate compiler-counter PR with behavior tests. |
| Done in this branch | Compiler/lexer/parser `this-escape` warnings | `this-escape-tally.md`, `compiler-lexer-parser-this-escape.md`, `fixed-in-this-branch.md` | Incremental/parallel compilation cannot rely on parser/compiler constructors dispatching into partially built AST/request objects. | Keep private lexer priming, lazy parser token priming, AST construction factories, and `CompilerThisEscapeConstructionTest` hook coverage. |
| Compiler cleanup backlog | Constant-folding conversion diagnostics | `compiler-lexer-parser-this-escape.md`, `Expression.convertConstant(...)`, `ConvertExpression.convertConstantValue(...)` | `System.err.println("No conversion found ...")` is not a compiler diagnostic and the multi-value conversion path preserves a legacy partial `Constant[]` with null slots. | Replace stderr with `ErrorListener` diagnostics or a structured compiler logger, then decide with focused tests whether failed multi-value folding should clear the whole constant array or report a hard compile-time error. |
| Done in this branch | Remove runtime `Container` constructor owner escapes | `this-escape-removal-audit.md`, `fixed-in-this-branch.md`, `ownership-diagnostics.md` | `new ConstHeap(this)`, field-initialized `new NativeTemplates(this)`, and base-constructor `registerContainer(this)` could expose a partially constructed owner. | Keep owner-explicit `ConstHeap`, lazy owner-local `NativeTemplates`, post-construction `MainContainer`/`NestedContainer` registration, and diagnostics that show registry membership plus explicit-owner heap state. |
| Done in this branch | `Parameter.cloneBody()` source mutation and copied deref state | `clone-usage-audit.md`, `fixed-in-this-branch.md` | The old clone method cleared deref state on the source and left copied method-owned register state on the clone. | `Parameter` no longer implements `Cloneable`; `copyFor(MethodStructure)` copies logical metadata for an explicit owner and drops the method-owned deref register. |
| Done in this branch | `MethodStructure.cloneBody()` cloned parameter ownership | `clone-usage-audit.md`, `fixed-in-this-branch.md` | Cloned parameters were assigned back to the original method owner via `param.setContaining(this)`. | `MethodStructure.cloneBody()` now copies parameters with `copyFor(that)`, and tests assert copied params/returns resolve through the clone. |
| Done in this branch | Delegated method `Parameter[]` element sharing | `clone-usage-audit.md`, `fixed-in-this-branch.md` | Delegated methods cloned only parameter array containers and shared mutable `Parameter` elements. | Synthetic delegated methods now use `createMethodCopyingParameters(...)`, which copies parameter elements for the new method owner before publication. |
| Done in this branch; remainder audited 2026-08-24 (five graduations below) | `ObjectHandle.cloneAs(...)` shallow runtime-handle copies | `clone-usage-audit.md`, `fixed-in-this-branch.md` | View/mask clones can share mutable field arrays and owner-bearing helper state. Cross-container movement was the highest-risk subcase because it could retain source-owner handles while changing the apparent owner. Same-owner inflated refs were also broken because clone-time `$outer` rewrites mutated refs visible through the source view. | This branch rejects direct cross-owner `GenericHandle.maskAs(...)` unless the handle graph is already shared with the target container, and `GenericHandle.cloneAs(...)` now keeps regular field storage shared while making inflated-ref `$outer` state view-local. The completion sweep (`clone-usage-audit.md`) covered all 88 handle classes and `relocateConst` inputs: cross-owner movement remains guarded; five single-container view-lifecycle gaps graduated to the must-fix row below. |
| Done in this branch | Same-owner `GenericHandle.cloneAs(...)` inflated-ref view backing | `runtime-metadata-op-cache-classification.md`, `clone-usage-audit.md`, `fixed-in-this-branch.md` | `ObjectHandle.cloneAs(...)` shallow-cloned the handle and shared the final field array. `GenericHandle` then rewired inflated `RefHandle.OUTER` entries in that shared array, so creating a same-owner access view mutated refs visible through the source view. | `GenericHandle` keeps the shared field array for regular values and adds sparse copy-on-write view overrides only for inflated refs that need a view-local `$outer`. `GenericHandleCloneAsTest.sameOwnerCloneKeepsInflatedRefOuterViewLocal()` fails on master by observing the source ref holder change to the clone, and passes here while proving regular field and referent write-through semantics. |
| Done in this branch | Default `Constant.copyForAdoption(...)` shallow clone | `clone-usage-audit.md`, `constant-pool-hostile-state-audit.md`, `constant-pool-must-audit-classification.md` | Master copied every new owner-local helper field unless a subclass remembered to override adoption. This branch makes `adoptedBy(...)` final, removes `Cloneable`/`Object.clone()` from `Constant`, and makes default `copyForAdoption(...)` fail closed. The condition family, array-backed byte value constants, immutable scalar value constants, composite value containers, parsed/delegated values, the `MatchAnyConstant` wildcard shell, `TerminalTypeConstant`, single-child type wrappers, storable relational type shells, dependant child/property type shells, recursive typedef shell, annotation/annotated-type shell, pseudo path/placeholders, named/type-backed identities, enum-value singleton subclass preservation, stateless type-sequence marker, pending/unresolved compiler placeholders, and transient cast-type guard have moved out of the default-clone bucket. | Keep the final wrapper, source-shape hook test, and validator; every future constant must implement explicit adoption or fail closed. |
| Closed 2026-08-25 for runtime-published mutation; structural snapshot follow-up remains | Complete `ConstantPool` ownership, mutation, and late-registration model | `constant-pool-state-audit.md`, `constant-pool-hostile-state-audit.md` | The pool is shared by compiler, linker, serializer, runtime, diagnostics, and JIT. Runtime entry now always marks publication, rejects new registrations, and rejects destructive recursive registration/optimization, module replacement, disassembly reload, and TypeInfo invalidation after publication. | Keep explicit owner parameters, scoped owner assertions, the always-on runtime publication marker, destructive-phase guard, and per-cache owner documentation. Remaining work is structural: split mutable compiler/linker pools from frozen runtime pools or add an equivalent immutable snapshot/freeze model; live-handle policy and metadata caches are tracked separately.. SNAPSHOT CLASSIFICATION LOCKED 2026-08-25 ("Post-Publication Residual Audit" in `constant-pool-state-audit.md`): the snapshot's READER-SAFETY half was MUST FIX and is CLOSED 2026-08-25 (ledger row 58): a volatile runtime read mirror (`m_aconstMirror`/`m_cconstMirror`, monitor-written, acquire-read count-then-array) now backs `getConstant(int)`/`size()`/`getConstants()` once a pool is runtime-published - an index below the count always resolves to a fully published element, and a racing index falls back to the pool monitor; the completion gate is unchanged. The frozen-base + runtime-annex + upstream-guard-scope architecture remains the SHOULD half. |
| Done in this branch (bridge fully deleted 2026-08-25) | Semantic `ConstantPool.getCurrentPool()` in constants and metadata helpers | `constant-pool-state-audit.md`, `constant-pool-hostile-state-audit.md`, `ambient-context-audit.md` | Hidden thread-local owner lookup can select the wrong pool or `null`; assertion-only owner checks are not production enforcement. | `getCurrentPool()` was removed first; on 2026-08-25 the entire scoped bridge followed - `withPool(...)`, `s_tloPool`, both transitional assertions, and the validation property are deleted, all 21 scopes unwrapped. `ConstantPoolDiagnosticsTest.currentPoolLookupGetterDoesNotExist()` now pins the whole family away by reflection. |
| Done in this branch for runtime-published registration and destructive mutation | Runtime-published pools can no longer register new constants after publication | `constant-pool-state-audit.md`, `constant-pool-hostile-state-audit.md` | `register(...)` and `ensure*Constant(...)` used to grow or rewrite pool state after a pool became container-visible unless a stress-only property was enabled. Runtime entry now always installs the publication marker; a genuinely new registration after that point fails before mutating `f_listConst` or lookup maps. This branch also prewarms private/protected/struct access constants before publication and rejects destructive compiler/serializer pool phases after publication. | Keep `ConstantPoolDiagnosticsTest.publicationMarkerIsInstalledByDefault()`, `runtimePublishedPoolRejectsRecursiveRegistrationPass()`, `runtimePublishedPoolRejectsModuleReplacement()`, `runtimePublishedPoolRejectsTypeInfoInvalidation()`, and `ClassCompositionLateRegistrationTest`. Continue the structural pool snapshot follow-up: immutable runtime storage, live `HandleConstant` policy, and owner-local metadata caches are not closed by the marker. |
| Done in this branch as a guard; must fix long term transactionally | `ConstantPool.register(...)` publishes constants before recursive registration completes | `constant-pool-hostile-state-audit.md`, `constant-pool-must-audit-classification.md`, `fixed-in-this-branch.md` | Master inserted a constant into list/map storage before `registerConstants(...)` and valid-pool checks finished. This branch preserves same-thread recursive lookup but makes public readers wait for completion. The guard's first shape had its own defect: the concurrent-insert branch awaited the winner's completion while holding the pool monitor, deadlocking the pool (full-suite hang in `MethodInfoTest`); fixed by waiting outside the monitor, proven by `ConstantPoolRegistrationDeadlockTest`. | Keep the completion guard, the reader-blocking tests, and the deadlock regression test; replace the guard with a private registration transaction/worklist that publishes only completed constants. The transactional rewrite is also what removes the remaining theoretical cross-thread wait cycle between two in-progress registrations that reference each other's constants. It should also retire the remaining unproven lock-free read: `f_listConst` is a plain `ArrayList` read without synchronization on the no-registration-in-progress fast path of `getConstant(int)`, safe today only by fragile piggyback publication rather than a JMM contract (the per-format constant maps are `ConcurrentHashMap` behind a volatile `EnumMap`, so those lookups are sound). Frozen runtime pools or a safely published append-only array closes it. The rewrite is also the place to add a typed canonical-lookup helper so the scattered unchecked `(T)` casts in `register(...)` collapse into one documented site. |
| Done in this branch | `TypeConstant.s_setRecursions` mutable static `HashSet` | `constant-pool-hostile-state-audit.md`, `fixed-in-this-branch.md` | A process-global `HashSet` was mutated during type relation recursion diagnostics; concurrent type checks could corrupt it even though the state was diagnostic-only. | Keep `ConcurrentHashMap.newKeySet()` plus `TypeConstantRecursionDiagnosticsTest`, which fails on the old `HashSet` shape and stresses parallel diagnostic additions. |
| Done in this branch | `TypeConstant` covariance/contravariance ambient pool lookup | `constant-pool-hostile-state-audit.md`, `ambient-context-audit.md`, `fixed-in-this-branch.md` | Type relation helpers silently used `ConstantPool.getCurrentPool()` to resolve auto-narrowing/generic helper constants; parallel containers or stale scoped bridges could select an arbitrary owner pool. | Keep explicit `ConstantPool` parameters and `TypeConstantOwnerApiTest`, which proves old ownerless signatures are gone and missing owners fail immediately. |
| Done in this branch | `TypeConstant` relation and formal usage cache keys | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md` | `m_mapRelations` cached context-sensitive auto-narrowing answers under a context-free left-type key, and `m_mapConsumes`/`m_mapProduces` cached access-sensitive formal usage under a name-only key with a thread-unsafe `IN_PROGRESS` sentinel. | Keep `TypeConstantOwnerApiTest.relationCacheSeparatesContextualAutoNarrowingQuestions()` and `formalTypeUsageCacheIncludesAccess()`. Relation keys now include the scoped context only when auto-narrowing can make it relevant; formal usage keys include `Access`, and private pending entries stop other threads from observing recursion sentinels. |
| Done in this branch | Numeric range folding ambient pool lookup | `constant-pool-hostile-state-audit.md`, `ambient-context-audit.md`, `fixed-in-this-branch.md` | `ByteConstant` and `IntConstant` range operations used a hidden current pool, so missing ambient scope crashed and stale scope registered `RangeConstant` values in the wrong owner. | Keep receiver-pool range construction and `ConstantRangeOwnerTest`, which covers no ambient pool and wrong ambient pool. |
| Done in this branch | `ConstantPool.checkFunctionCompatibility(...)` ambient pool lookup | `constant-pool-hostile-state-audit.md`, `ambient-context-audit.md`, `fixed-in-this-branch.md` | An instance method on `ConstantPool` ignored the receiver and asked thread-local state for `Tuple<>`; missing or stale ambient scope changed or crashed the answer. | Keep receiver-pool `typeTuple0()` and the focused `ConstantPoolDiagnosticsTest` no-ambient compatibility case. |
| Done in this branch | Nested identity generic resolution ambient pool lookup | `constant-pool-hostile-state-audit.md`, `ambient-context-audit.md`, `fixed-in-this-branch.md` | `resolveNestedIdentity(pool, resolver)` accepted an explicit output pool but resolver-backed nested identities later ignored it and used `ConstantPool.getCurrentPool()`. | Keep resolver-backed nested identities carrying the explicit output pool and `NestedIdentityOwnerTest`, which proves a wrong ambient pool is ignored. |
| Done in this branch | Method/property metadata ambient pool helpers | `constant-pool-hostile-state-audit.md`, `ambient-context-audit.md`, `fixed-in-this-branch.md` | `MethodBody`, `MethodInfo`, and `PropertyInfo` used hidden current-pool state while resolving annotations, narrowing signatures, or adopting shared property identities. | Keep receiver-owner pool derivation and the no-ambient metadata owner tests in `MethodInfoTest` and `TypeInfoMemberOwnershipTest`. |
| Done in this branch | Cycle-safe `MethodBody` target equality and hashing | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md`, `test-failure-evidence.md` | `MethodBody.hashCode()` hashed only `m_id`, while `equals()` recursively compared `FromInto`/`Implicit`/`Union` target `MethodInfo` graphs. Legal cyclic metadata could therefore overflow the stack during hash collection lookup, and parallel connector loading made the failure reproducible. | Keep stable target-shape equality/hash semantics and `MethodInfoTest.methodInfoEqualityDoesNotRecurseThroughMethodTargets()`. `InterpreterConnectorTest.parallelConnectorsLoadIndependentNativeContainers()` exercises the real connector path. |
| Done in this branch | Safe publication for optimized method/property chains | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md`, `test-failure-evidence.md` | `MethodInfo.m_aBodyResolved` and `PropertyInfo.m_chainGet/m_chainSet` were plain lazy arrays even though first construction can mutate/attach runtime metadata. The property caches were also key-incomplete for non-null nested ids. | Keep synchronized/volatile first publication for hot top-level caches, capped-chain recursion outside the method monitor, and focused parallel first-access tests. If nested property ids become hot, add a real keyed cache instead of reusing the top-level slot. |
| Done in this branch | Safe publication for `TypeInfoReal` derived metadata caches | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md`, `test-failure-evidence.md` | `TypeInfoReal` published derived name/signature `HashMap` instances, delegate-only TypeInfo graphs, and readiness flags through plain fields. `ensureCaches()` also used double-checked locking without a volatile flag. | Keep immutable property-name snapshots, synchronized expanding method-signature caches, synchronized/volatile first publication, retry-on-failure child-newability semantics, and `TypeInfoMemberOwnershipTest.derivedTypeInfoCachesAreSafelyPublishedInParallel()`. |
| Done in this branch | Safe publication for `PropertyInfo` helper cells | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md`, `test-failure-evidence.md` | `PropertyInfo` cached ref annotations, injected/implicitly-assigned booleans, base Ref/Var type, and getter/setter method constants through plain lazy fields. The type and method helpers can intern owner-pool constants, and the annotation helper exposed one cached array without a memory edge. | Keep synchronized/volatile first publication, one helper value per owned property, the detached cached annotation snapshot, and `TypeInfoMemberOwnershipTest.propertyHelperCachesAreSafelyPublishedInParallel()`. |
| Done in this branch | Safe publication for `ClassComposition` field layout, field-name, and initializer helper cells | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md`, `test-failure-evidence.md` | `ClassComposition` field layout was a loose group of map/count/flag fields copied into access views at construction time, and it cached owner-bearing `StringHandle[]` field names and synthetic structure initializers through plain lazy fields. Field-name arrays were duplicate clone-local cells, and initializer cells could copy whichever inception value existed at access-view clone construction time. | Keep inception-owned final `Lazy.Owner` cells, immutable field-layout map shape, access-view delegation, lazy no-field initializer behavior, and `ClassCompositionSafePublicationTest.accessViewsShareSafelyPublishedInceptionRuntimeCaches()`. |
| Done in this branch | Final owner-lazy `PropertyComposition` struct view | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md`, `test-failure-evidence.md` | `PropertyComposition.ensureAccess(STRUCT)` cached a runtime composition identity through a plain mutable lazy field. Parallel first access could publish duplicate struct-view identities, and the old shape kept another ad hoc nullable cache. | Keep final `Lazy.Owner` struct-view cache, final inception/struct role fields, and `ClassCompositionSafePublicationTest.propertyCompositionStructViewIsOwnerLazyAndShared()`. |
| Done in this branch | `FileStructure` ambient error-listener fallback | `ambient-context-audit.md`, `constant-pool-hostile-state-audit.md`, `fixed-in-this-branch.md` | `FileStructure.getErrorListener()` could send diagnostics to a wrong ambient pool's listener or crash when no pool was bound. | Keep file-owned diagnostics and `FileStructureTest.errorListenerIgnoresAmbientPool()`. |
| Done in this branch (2026-08-25: bridge deleted entirely) | `ConstantPool.getCurrentPool()` compatibility API and the `withPool(...)` scoped bridge | `ambient-context-audit.md`, `scoped-value.md` | Keeping any getter or install API made hidden owner lookup easy even after current call sites were fixed. | No ambient pool state exists any more: `getCurrentPool()`, `withPool(...)`, `s_tloPool`, `assertCurrentPool*`, and `xvm.asm.validateConstantPoolCurrentScope` are all deleted; every former scope runs on explicit owners. Guarded by `ConstantPoolDiagnosticsTest.currentPoolLookupGetterDoesNotExist()` (reflection pins on the whole family). |
| Done in this branch | `TypeConstant.m_handle` runtime TypeHandle cache | `runtime-metadata-op-cache-classification.md`, `constant-pool-hostile-state-audit.md`, `fixed-in-this-branch.md` | `TypeHandle` is owner-bearing runtime state, but the old cache key was only the `TypeConstant`. Two containers sharing one pool could reuse the first container's Type handle. | Shared Type handles are now cached by `Container.ensureTypeHandle(TypeConstant)` in a concurrent owner-local map. `TypeConstant.ensureTypeHandle(Container)` still handles pool registration and foreign uncached handles. |
| Closed 2026-08-24 as an audit (one graduation below) | Live runtime handles embedded in constants | `clone-usage-audit.md` ("Rows 125/161 Completion Sweep") | `HandleConstant` wraps `ObjectHandle`, owner-specific runtime state. | All 11 boundary paths classified: adoption/re-adoption/annotation-param/merge paths reject; the single creation site and first registration are same-owner by construction; ConstHeap relocation, serialization, and JIT paths are proven unreachable (the `FrameDependentConstant` short-circuit, zero-ref `optimize()` drop, zero JIT references). One bounded hazard graduated to the must-fix row below; one latent should-fix recorded (inherited `assemble()` would write a corrupt single byte if a HandleConstant were ever referenced at write time - override to throw). |
| Fixed in this branch (2026-08-25 same day as graduation; logged S19, launch row L18) | `ConstantPool.f_implicits` was a plain `HashMap` written from concurrent service threads | `constant-pool-state-audit.md`, 2026-08-25 per-pool lazy-cache audit | `getImplicitlyImportedIdentity` reads (:1474) and lazily writes (:1509) a plain `HashMap` (:4338). Runtime callers hit it from service threads today (`xService.java:580` resolving `Timeout`, `TypeConstant.java:737`, TypeInfo builds), and all ServiceContexts of a container share one pool over a multi-thread executor, so a racing resize can structurally corrupt the map - lost unrelated entries, broken bins - not just duplicate work. | Fixed: converted to `ConcurrentHashMap` (values converge to interned identities, so racing puts are benign once the map itself is safe); pinned by `ConstantPoolDiagnosticsTest.implicitIdentityCacheIsConcurrentSafe`. |
| Fixed in this branch (2026-08-25, same-day fix wave) | Native injection `ensure*` caches raced duplicate service creation | `manual-lazy-cache-audit.md` ("Completion Sweep" section) | `xRTAlgorithms.m_hAlgorithms`, `xLocalClock.m_hLocalClock`/`m_hUTCClock`, `xTerminalConsole.m_hConsole`, `xInjector.m_hInjector`, `xRTRandom.m_hRandom`, and the `NativeContainer.ensureOSStorage`/`ensureFileStore`/dir/network family are double-read lazy caches on SHARED template/container receivers whose suppliers are non-idempotent: each racing first injection calls `createServiceContext`, and the losers' duplicate native services (two Consoles, two Clocks, a second OSStorage) stay registered forever. Owner-consistent but divergent; reachable today with concurrent first injections from two service threads. Master-verbatim shape. | Fixed per shape: DCL over volatile fields for the synchronous sites (Console incl. line-reader init, both clocks, Random, Injector); first-wins publication with loser-context `terminate` for the construct-request sites (OSStorage - the continuation publishes the RESOLVED handle and the loser's context is unregistered; Algorithms - which additionally CACHED A FRAME-BOUND DeferredCallHandle on the R_CALL path, handing later callers a deferred tied to the first caller's frame; the networks had the same deferred-caching defect); fs-derived property caches publish first-wins convergent. `NativeInjectionSingletonTest.racingFirstInjectionsCreateExactlyOneService` - 8 latch-raced first injections, one identity, registry grows by exactly one - red-verified against the plain shape. Full 22-module sweep green. |
| Fixed in this branch (2026-08-25, graduated by BOTH completion audits independently) | `ConstantPool.infoPlaceholder()` identity race stranded TypeInfo placeholders | `constant-pool-state-audit.md` ("Post-Publication Residual Audit"), `manual-lazy-cache-audit.md` ("Completion Sweep") | The placeholder was created by a plain lazy write, but `TypeConstant.clearTypeInfoPlaceholder` CASes the type's `m_typeinfo` slot against `pool.infoPlaceholder()` BY IDENTITY: two racing first TypeInfo builders created two placeholder instances, and a slot holding the loser could never be cleared - permanently "being built", with `ensureObjectTypeInfo` reporting the spurious root-Object failure. Master-verbatim shape. | Fixed: identity-stable placeholder installation; shape/race pin in `ConstantPoolDiagnosticsTest`. |
| Should fix (2026-08-25 completion audits, one batch wave) | Publication-visibility and guard-symmetry batch | `constant-pool-state-audit.md` residual section, `manual-lazy-cache-audit.md` completion sweep, op-cache doc completion section | Convergent-but-unfenced publication and small guard gaps: relation-cache invalidation stamping; `m_fNative`/`MethodBody.m_impl` volatiles; recursive `computeIfAbsent` in `f_mapRefTypes`/composition chain maps; `setNakedRefType` destructive-guard symmetry; upstream/child pools never `markRuntimePublished` (promote to must-fix with multi-container/LSP support); `StringHandle.m_hash` + literal `m_hText` + `PropertyInfo.m_jmd*` + `m_sJitName` + `Container.ensure*` + template `m_typeCanonical` volatiles; `m_cVars` acquire hole; `m_ast` lazy decode; `m_FUsesSuper`/`m_safety`/`m_structFinally`/`m_FHasCode`. | One mechanical volatile/DCL wave with shape pins; the relation-stamp and recursive-computeIfAbsent items need small structural edits. |
| Closed 2026-08-25 (one fix; warm-at-publication considered and rejected) | Per-pool lazy singleton caches | 2026-08-25 per-pool lazy-cache audit, `constant-pool-state-audit.md` | The ~180 lazy `m_type*`/`m_clz*`/`m_val*`/`m_sig*` cells all converge to canonical interned instances via the hardened `register()` (racing writers store the same reference), leaving only benign duplicate work plus a no-happens-before technicality; a post-publication cold first-intern fails loudly via the marker, which is the guard doing its designed job. `m_typeNakedRef` (startup handoff HB) and `f_setValidPools` (compile-phase-only) are CONFINED with evidence; `m_setJitPrimitives` is JIT-startup-confined. | Fixed the one real defect: `m_valDefault` was the sole cell bypassing interning (racing inits yielded distinct equal objects) and now routes through `register()`. A prewarm-all-cells pass was prototyped and REJECTED at user direction as brittle (a hand-maintained ~180-call list plus reflection ratchet plus linked/unlinked special-casing, buying no correctness - the races are benign-convergent and the cold-accessor trip is the intended loud diagnostic). |
| Done in this branch | Static mutable implicit metadata maps | `constant-pool-state-audit.md` | Class initialization safely published the map references, but `HashMap` contents remained mutable process-wide state. | `s_implicits` and `s_implicitsByPath` are now frozen with `Map.copyOf(...)` after parser arrays are cloned, and the unused `UnionTypeConstant.SpecialFunkies` mutable set was removed. Keep `ConstantPoolDiagnosticsTest.staticImplicitMetadataMapsAreImmutable()`. |
| Done in this branch for runtime-published pools; compiler/JIT backlog for phase ownership | Destructive pool optimization, module replacement, and disassembly/assembly mutation | `constant-pool-state-audit.md` | `optimize()`, `replaceModule(...)`, and serialization/disassembly paths mutate pool positions, contents, and caches; they are not safe as shared runtime operations. Runtime-published pools now reject those paths before mutation. | Keep mutable compiler pools separate from frozen runtime pools; incremental compiler work should own this isolation and the future immutable snapshot model. |
| Done for first publication; one lifecycle path graduated to must-fix 2026-08-25 (row below) | `MethodStructure.Code` and decoded op address/link publication | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md`, 2026-08-25 frozen-code lifecycle audit | Address/link fields are method-owned, not owner-bearing handle caches, but a plain `m_code` publication cell could expose partially linked decoded code or duplicate decoded op graphs during parallel first `getOps()`. The full-lifecycle audit proved every other post-publication writer safe by ordering: disassembled methods never reassemble (`needsReassembly` requires `m_abOps == null`), link-phase `forceAssembly` completes before execution, `createInitializer` stays unattached until assembled, and `markNative`/`resetRuntimeInfo` only replace the volatile cell during pre-dispatch bootstrap. | Keep volatile/synchronized first publication, runtime-ready diagnostics, and `OpRuntimeCacheTest.methodCodeIsSafelyPublishedAndLinkedBeforeRuntimeAccess()`. The one unguarded writer - runtime-lazy delegation synthesis - is the graduated must-fix row below. The structural `ResolvedCode`/freeze-boundary refactor stays the should-fix follow-up. |
| Fixed in this branch (2026-08-25; graduated same day from the frozen-code audit) | Runtime-lazy delegation synthesis publishes half-built method code | `ClassStructure.ensureMethodDelegation` / `ensurePropertyDelegation`, `MethodInfo.buildOptimizedMethodChain`, `PropertyInfo.createDelegatingChain`, `DelegationSynthesisTest` | Two proven defects. (1) The unsynchronized check-then-create published the synthetic method as a findable child BEFORE its `Code` was assembled: two MethodInfo/PropertyInfo owners for one host class+signature dispatching concurrently let the loser capture a half-built method and race the winner's in-place `forceAssembly` (the nested `ensureMultiMethodStructure` check-then-create raced the same way, returning null to the loser - reproduced by the concurrent test). (2) The branch's always-on publication marker turned LEGITIMATE lazy delegation into a loud `assertRegisterBeforeRuntimePublished` ISE - proven deterministically red on the pre-fix shape ("ConstantPool registered Property{value} after runtime publication"), i.e. a branch-enablement regression the guard family introduced, now closed. | Fixed, lock-free at the caller (user direction: no synthesis lock object): synthesis builds the method DETACHED (new `MultiMethodStructure.createDetachedMethod*` factories reusing the `createInitializer` precedent), assembles completely, then publishes through atomic first-wins copy-on-write primitives that live WITH the data structure - `Component.publishRuntimeChild`, `Component.ensureRuntimeMultiMethodStructure`, `MultiMethodStructure.publishOrAdoptSynthesizedMethod`, each synchronized on the component's own reentrant leaf-scoped monitor (the same precedent as `ensureChildByNameMap()`), with `m_childByName`/`m_methodByConstant` volatile so unsynchronized readers only ever observe a complete map. Synthesized identities are interned by signature, so id-based adoption is exact; racing builders discard their build at publication; no lock is held across TypeInfo construction, so cross-class synthesis recursion cannot deadlock. The marker interaction is an explicit per-thread reference-counted `ConstantPool.openRuntimeSynthesisWindow(...)` that relaxes only the registration guard, never the destructive-mutation guard (the fully stateless alternative - threading an explicit permit through every `ensure*Constant` signature - is recorded as the eventual explicit-owner ideal). `DelegationSynthesisTest`: deterministic marker red, find-stability, and an 8-thread synthesis hammer; red-verified by stashing the fix. |
| Done in this branch | `GuardStart.m_guard` / `GuardAll.m_guard` runtime descriptor lazy writes | `runtime-metadata-op-cache-classification.md`, `fixed-in-this-branch.md` | Guard descriptors are decoded method metadata. Building and publishing them from `process()` made the shared op mutate during runtime first execution. | Keep link-time descriptor construction plus the local fallback that does not write back to the op. `OpRuntimeCacheTest.guardDescriptorsAreLinkedBeforeProcessAndNotPublishedByProcess()` guards the source shape. |
| Classified 2026-08-25; runtime/ASM subitems fixed, JIT/compiler/should-fix follow-ups remain | Owner-bearing type/class metadata caches | `manual-lazy-cache-audit.md`, `state-inventory.md`, `runtime-metadata-op-cache-classification.md` | `ClassComposition`, `TypeConstant`, `TypeInfoReal`, and similar caches may be owner-local or shared; the API often does not say. | Runtime/ASM metadata caches in this umbrella are classified: `ClassComposition`, `TypeInfoReal`, `MethodInfo`, `PropertyInfo`, `TypeConstant.m_mapRelations`, `m_mapConsumes`, `m_mapProduces`, and `m_typeNormalized` now have owner/key/publication proof or fixes. JIT-name/descriptors are parked in JIT rows; compiler metadata stays on the compiler branch; The `ChildInfo.f_setIds` exposure and `withPool(...)` removal cleanups both closed 2026-08-25. |
| Closed 2026-08-24 (classified) | Raw/unchecked async/service/native-template/JIT metadata paths | `generics-api-audit.md` ("Must-Audit Row 132 Classification"), `lint-parallelism-risk-audit.md` | Erasure hides whether futures, responses, op-info caches, reflected templates, and type metadata carry the correct owner/payload. | Full classification: 288 unchecked/rawtypes warnings enumerated (the naive lint run truncates at javac's default `-Xmaxwarns 100`; use `-Porg.xtclang.java.maxWarnings` for real sweeps). 18 owner-sensitive site groups audited in depth: 9 proven confined (with recorded proofs - e.g. all `Message.f_future` completions are scheduling-lock-confined, and `Container.getRuntimeOpCache(op, category, Class<T>)` is the model typed checked boundary), 9 need typed boundaries, of which 6 were already tracked and 3 are new (raw `Utils.ANY` predicate, the JIT parent-to-child injection supplier needing a `TypeConstant`-checked owner-asserted boundary, `BinaryAST.readAST` needing a `Class<N>` check). No reachable cross-owner delivery was found; the worst shape is `ServiceContext`'s erased `f_future` completed with a tuple handle where the binding claims `ObjectHandle[]` - lock-confined, but the raw cast at the consumer launders the shape lie and belongs to the typed-boundary work. |
| Closed 2026-08-24 (84/84 classified; fatal gate active) | Fallthrough lifecycle/state-machine switches | `lint-parallelism-risk-audit.md` ("Row 133 Fallthrough Classification"), fallthrough gate in `org.xtclang.build.java.gradle.kts` | State-machine fallthrough in runtime/compiler/JIT code can skip or repeat owner-sensitive stages, and the language makes accidental and intentional falls syntactically identical. | Every site in the composite build (84: 81 main + 3 GC tests) was read and classified INTENTIONAL - the feared dispatch sites are documented protocol (`R_RETURN_CALL -> R_CALL` pop-then-call, the `ixStep++` construct sequencer, `stageNext` machines). Four missing markers were added; 35 methods carry `@SuppressWarnings("fallthrough")` as the reviewed-and-proven marker. `-Xlint:fallthrough` is now fatal alongside this-escape, so a future forgotten `break` fails the build instantly. The arrow-switch migration row below shrinks the suppression list over time. |
| Closed 2026-08-24 (all sites classified) | Ambient `ThreadLocal`/`TransientThreadLocal`/`ScopedValue` state | `ambient-registry-must-audit-classification.md` ("Row 134 Completion Sweep"), `scoped-value.md`, `state-inventory.md` | Hidden owner context can leak across pooled threads and async callbacks. | Completion sweep classified every ambient mechanism (10 owner-context mechanisms, 6 noise/guard hits; zero previously-unknown owner-bearing sites). No set-without-guaranteed-clear exists in the ThreadLocal class: all `withPool` scopes are try-with-resource, deferred-list drains restore in finally, and the old "stale deferred work silently processed" verdict is corrected - current code fails loud on stale state. The remaining category-(c) groups are tracked by named slices: JIT static `Ctx.get()` captures (the JIT must-fix rows), ServiceContext diagnostic readers, and unasserted compiler-side `withPool` scopes. |
| Escape fixed in this branch; remaining shell scope reduced | JIT `Xvm` startup owner publication | `jit-global-owner-classification.md`, `plans/jit-xvm-owner-refactor.md`, ledger row 37 | `Xvm` passed partially constructed `this` into `NativeTypeSystem.create(...)`, and `TypeSystem`/loader setup could reach facade fields that were not assigned yet. | Done: private constructor + static `Xvm.create(repo)` boot factory + one volatile immutable `Boot` record behind accessors; the this-escape suppression is dead and `JitConstructorEscapeTest` guards the shape. The owner-shell plan's remaining substance is the generated-static and bridge ambient-`Ctx` rows below, not the facade construction. |
| Must fix, open (graduated 2026-08-24 at user direction) | JIT generated `<clinit>` owner-bearing statics | `jit-global-owner-classification.md`, `ambient-registry-must-audit-classification.md` | Generated classes can write constants, injected static properties, and singleton `$INSTANCE` values from `Ctx.get()` into classloader-wide static fields. The first active container/classloader context can become permanent state for later containers. Classification: this is the static-scope analogue of `this-escape` - owner state escaping its owner's lifetime into JVM-global classloader scope. It breaks parallel containers AND same-JVM sequential runs (a second world inherits the first world's statics); it is reentrancy-enablement, not a single-run master bug, because master runs one world per JVM. Scheduling note: the JIT is under active upstream churn, so these rows should be fixed against a rebased JIT snapshot to avoid conflict waste. | Prove the generated classloader is single-owner for each static, or move owner-bearing values into `TypeSystem`/`Container` tables. Add two-container and parallel class-initialization tests plus source-shape guards for generated static `Ctx.get()` use.. Integrated 2026-08-25 into the master bug plan: submissions rows 21-24 (launch rows J21-J24), classified already-broken-single-threaded vs broken-under-concurrency; shape pins in `JitConcurrencyHazardCensusTest`; full architecture/dependency report in `jit-runtime-report.md` (section 4 maps the exact JIT->asm unsafe-surface edges for later patching). |
| Must fix, open (graduated 2026-08-24 at user direction) | JIT bridge static enum/class singletons and static ambient `Ctx` use | `jit-global-owner-classification.md`, `ambient-registry-must-audit-classification.md` | Bridge statics call `Ctx.get()`, `$ctx()`, or `$xvm()` during static initialization and store metadata such as `TypeConstant`. Static initialization can capture the first active owner or fail outside a bound `Ctx`. | Move bridge enum/class singletons behind native-type-system or container/type-system lookup, or prove construction is native-classloader-owned without ambient context. Add a source-shape guard for static ambient `Ctx` use in `javatools_jitbridge`.. Integrated 2026-08-25 into the master bug plan: submissions rows 21-24 (launch rows J21-J24), classified already-broken-single-threaded vs broken-under-concurrency; shape pins in `JitConcurrencyHazardCensusTest`; full architecture/dependency report in `jit-runtime-report.md` (section 4 maps the exact JIT->asm unsafe-surface edges for later patching). |
| Classified 2026-08-25; parked for JIT bridge/API PR | JIT bridge ambient helper API | `ambient-registry-must-audit-classification.md`, `jit-global-owner-classification.md` | `nObject.$ctx()`, `$xvm()`, and `$owner()` hide owner selection; `$owner()` combines the current `Xvm` with an encoded owner id, so a wrong ambient context can resolve the wrong container. The completion read also found bridge methods and constructors using `$xvm()`/`$owner()` for metadata and owner lookup (`TerminalConsole`, `ContainerControl`, `Nullable`, `Ordered`, `String`, `Array.Mutability`, `FPNumber.Rounding`). | This is a JIT-scoped must-fix-by-API, not an interpreter/runtime fix: replace helpers with explicit `Ctx` parameters or assert that an object's encoded owner belongs to the active `Ctx`/`Xvm`. Add unbound/wrong-context bridge tests and source-shape guards for static `Ctx.get()`, `$ctx()`, `$xvm()`, and `$owner()` captures before claiming JIT reentrancy.. Integrated 2026-08-25 into the master bug plan: submissions rows 21-24 (launch rows J21-J24), classified already-broken-single-threaded vs broken-under-concurrency; shape pins in `JitConcurrencyHazardCensusTest`; full architecture/dependency report in `jit-runtime-report.md` (section 4 maps the exact JIT->asm unsafe-surface edges for later patching). |
| Classified 2026-08-25; parked for JIT owner/static PRs | JIT owner/static/runtime interaction | `jit-implications.md`, `scoped-value.md`, `jit-global-owner-classification.md` | JIT has its own generated statics, `Ctx.Current`, classloader scope, bridge module assumptions, and owner registries. The completion read confirmed generated `<clinit>` obtains `Ctx.get()` and writes classloader statics for `$scN` constants, injected static properties, and `$INSTANCE` singletons; bridge enum/enumeration statics can initialize from `Ctx.get()`/`$ctx()`/`$xvm()`; `OpCondJump.buildUnary(...)` evaluates link-time conditions through `Ctx.get().container`. Interpreter ownership changes do not make these JIT statics safe. | Keep lifecycle, generated-static policy, bridge ownership, and `Ctx` build-context removal as separate JIT follow-up PRs. Required proof: classloader/type-system owner checks for generated statics, two-container and parallel JIT tests with different injectors, source-shape guards for ambient static captures, and a JIT ownership dump rooted at `Xvm`.. Integrated 2026-08-25 into the master bug plan: submissions rows 21-24 (launch rows J21-J24), classified already-broken-single-threaded vs broken-under-concurrency; shape pins in `JitConcurrencyHazardCensusTest`; full architecture/dependency report in `jit-runtime-report.md` (section 4 maps the exact JIT->asm unsafe-surface edges for later patching). |
| Closed 2026-08-25 (audit; dissolved into the two concrete rows below) | Public/protected arrays, mutable static collections, and broad public/protected fields | `state-inventory.md`, `array-element-exposure-audit.md`, 2026-08-25 shared-mutable-surfaces audit | The integration audit re-verified the array-exposure census (element-write scan clean today) and swept the remaining named groups: no owner-bearing shared mutable state survives outside the entries below. The only mutable statics left in javatools are `Token`'s three clinit maps, `NativeNames.reservedMethodName`, and `BinaryAST.ALREADY_DISPLAYED`; `ListMap.EMPTY` is construction-immutable and `xService.ATOMIC_PROPERTIES` is already `Set.of`. | Subsumed by the census plus the two rows below; nothing further to audit. |
| Verification backlog; FULL-SWEEP MILESTONE GREEN 2026-08-25 | Same-JVM direct/Gradle plugin repeated execution and stress benchmarks | `plans/same-jvm-launcher-stress.md` | The original user pain is repeated execution in one JVM without stale global state; unit tests alone do not prove that. 2026-08-25: `runDirectSequenceStress` passes the FULL curated known-runnable set (all 22 modules x 2 iterations, 44 sequential same-JVM runs) with curated ownership validation PLUS the foreign-reference reachability sweep enforced on every windowed container. Getting there surfaced and fixed, in order: the mechanism-4 array-view path, the thread-scoped window recalibration, the guard-diagnostics recursion and masking, two validator policy calibrations (native-parent currency, walk-root lifetime), the pending-future sweep reclassification, and the constructed-lazy-ref publication redesign (TestLiterals/TestServices regression pair, ledger row 53). | Remaining: Gradle direct-mode repeated execution coverage, parallel same-JVM execution, benchmark comparison vs forked JVM (plan doc). |
| Done in this branch | `this-escape` lint is a build error across the composite Gradle build | `this-escape-tally.md`, `build-logic/common-plugins/src/main/kotlin/org.xtclang.build.java.gradle.kts` | New constructor escapes now fail compilation immediately instead of appearing as advisory warnings, in every Java source set the shared convention applies to. | The convention plugin appends `-Xlint:this-escape` after the default `-Xlint:none` (javac lint flags are cumulative), and the pre-existing default `-Werror` makes exactly that one category fatal without changing any other lint severity. `./gradlew build` is green with the gate active. There are now zero suppressions: the final exception (the `Xvm` startup constructor) was eliminated by the boot-factory refactor (ledger row 37). |
| Done in this branch (wave); remaining rows tracked in `logging-diagnostics-audit.md` | Terminal failure boundaries: no print-and-continue at artifact/execution mutation points | `exception-hygiene-audit.md`, `logging-diagnostics-audit.md`, `runtime-ownership-hardening-ledger.md` rows 24-28 | Compiler codegen, method op assembly, the interpreter op loop, async future completion, and queued file writes could print a failure and keep going, persisting corrupt artifacts or reporting success after defects. | Keep the terminal-boundary fixes and their red-on-master tests (`CompilerCodegenFailureTest`, `MethodStructureAssemblyFailureTest`, `RuntimeFailurePropagationTest`, `FutureCompletionSafetyTest`, `RawOSFileChannelSubmitTest`). Requested-load cause preservation is now also done (`ModuleLoadException`, `ModuleRepositoryLoadFailureTest`), manual-test runner pass/fail authority is verified machine-visible on every path (`FailProbe`, ledger row 33), and jsondb rollback-failure retention is fixed (ledger row 34, single hunk, master-applicable). The terminal-failure-boundary family is fully closed. |
| Done in this branch (master-portable) | `ServiceContext.m_mapCallbacks` alarm-callback registry race | `state-inventory.md` ("Row 160 Weak/Identity Registry Sweep"), `WeakCallback.java`, `xLocalClock.java`, `xNanosTimer.java` | The registry is a plain lazily-created `HashMap`. The owning service `put`s under its scheduling lock (`WeakCallback` ctor), but alarm maturation calls `extractCallback()` -> `HashMap.remove` on the process-wide static `ecstasy:LocalClock` Timer thread with no common monitor. A `put` resize racing a timer-thread `remove` corrupts the map; a lost entry later throws `IllegalStateException` on the timer thread, and an uncaught TimerTask exception kills the shared static Timer - silently disabling every alarm in every container. Secondary defect: alarm cancel never removes its `Callback` entry, leaking the captured `Frame`/`FunctionHandle`. | Fixed: eager final `ConcurrentHashMap` registry, null-returning `extractCallback()` (no timer-thread throw), null-tolerant fire paths, and `discard()` on both cancel paths (`xLocalClock` gated on `TimerTask.cancel()` truth, `xNanosTimer` inside the `m_fDead` monitor). Guarded by `NativeCallbackRegistrationTest.callbackRegistryIsConcurrentTimerSafeAndLeakFree()`; ledger row 35. The identical shape exists on master, so this ports as a small standalone PR. |
| Done in this branch (guard master-portable) | `ConstHeap` serves live `HandleConstant` values raw across sibling containers | `clone-usage-audit.md` ("Rows 125/161 Completion Sweep"), `ConstHeap.java:49`, `HandleConstant.java` | The `FrameDependentConstant` short-circuit returns `m_hValue` to any frame whose pool resolves the constant, with no `isShared(frame.container())` check. Two containers over one loaded module (the same-JVM reuse target) share the module pool, so a sibling instantiating the annotated type receives the creator's live handle, bypassing maskAs/proxy isolation. | Fixed: `HandleConstant.getHandleFor(Container)` guards the serve - the owner always gets its own handle, any other container only what could legally cross a service boundary (non-service, pass-through); `assemble()` now fails loudly. `HandleConstantOwnerGuardTest` proves owner-serve, cross-container mutable rejection, and pass-through immutable serve with two real native containers; `HandleConstantAssembleTest` proves serialization refuses. Ledger row 36. |
| Done in this branch (all five mechanisms) | Handle view-clone lifecycle desyncs (five mechanisms) | `clone-usage-audit.md` ("Rows 125/161 Completion Sweep") | All five are single-container gaps where a `cloneAs` view forks per-instance state that must stay logically shared: (1) GUARDED (ledger row 40, `RefViewGuardTest`): register-bound refs now refuse view cloning fail-loud - REF_REF delegation is the designed second-handle mechanism, and no legitimate clone path was traced; (2) FIXED (ledger row 39, `AtomicViewSharingTest`): the guard state now lives in a constructor-final shared `InitGuard`; classified reentrancy-enablement - on master the inflated ref was a single shared instance, and multiple view instances only exist because of this branch's correct view-local `$outer` fix; (3) FIXED (ledger row 38, `AtomicViewSharingTest`): the atomic and injected cells are now constructor-final and shared, per the immune `AtomicHandle`/`FutureHandle` pattern; (4) GUARDED (ledger row 41, `ArrayViewGuardTest`): Mutable arrays refuse view cloning fail-loud (`clear()` is the single in-place delegate replacement; delegate-internal growth is shared through the delegate object, so Fixed/Persistent/Constant views stay consistent and still clone); (5) FIXED (ledger row 42, `FreezeViewSharingTest`): freeze state migrates into a CAS-installed cell shared by all views; the flag is encapsulated with constructor-only direct writes and a ratchet test pinning that discipline. | All five closed: mechanisms 2/3/5 by shared cells (`InitGuard`, eager atomic/injected cells, `FreezeCell`), mechanisms 1/4 as fail-loud guards (register-bound refs and Mutable arrays refuse view cloning - REF_REF delegation and non-Mutable mutabilities are the designed alternatives). Ledger rows 38-42; every mechanism has a two-view test, and master-reachability is stated per row (3 = master bug via the cross-service install race; 5 = verbatim master shape, reachable when views coexist at freeze; 2 = branch-enablement; 1/4 = latent, closed fail-loud). |
| Fixed in this branch (2026-08-24, fail-loud guards; structural alternative rides with the clone-eradication study below) | Freeze-split residuals in non-`GenericHandle` view-capable handles: `TupleHandle`, `ArrayHandle` (`m_mutability`), `FunctionHandle` | `runtime-ownership-hardening-ledger.md` row 42, `clone-usage-audit.md` | The shared `FreezeCell` lives in `GenericHandle`, but `TupleHandle` (xTuple.java:679) and `ArrayHandle` (xArray.java:967) extend `ObjectHandle` directly, share element storage across views, and keep per-view lifecycle state. `ArrayHandle` additionally keeps the `Mutability` enum per view, and every write-permission check in `xArray` reads the handle's enum, not the shared delegate's - so a freeze through one Fixed/Persistent array view leaves the sibling view still willing to write into the frozen shared storage (the mechanism-4 guard refuses only `Mutability.Mutable` clones). `FunctionHandle` extends `SignatureHandle` -> `ObjectHandle` with the same per-view flag. Same failure class as ledger row 42; the fact that the fix must be re-implemented per subclass is itself evidence for the clone-eradication study below. | Shipped 2026-08-24 as fail-loud guards: `ArrayHandle.cloneAs` now refuses everything but `Mutability.Constant` (the mechanism-4 guard covered only `Mutable`; Fixed/Persistent split `m_mutability`/`m_fMutable` on freeze while `xArray` write checks read the handle enum); `TupleHandle.cloneAs` and `FunctionHandle.cloneAs` refuse while `isMutable()`. Two-view red/green tests: `TupleViewGuardTest.mutableTupleRefusesViewCloning`. The `FunctionHandle` guard is defensive - constructing a mutable bound function in a unit test needs frame machinery, so it has no two-view test yet; any natural path that clones a mutable function view now fails loud instead of desyncing. **ArrayHandle graduated to the structural closure 2026-08-25:** the same-JVM stress harness hit the array guard on an ORDINARY path (`MOV_THIS_A #1, PRIVATE` creating a this:private view of a live mutable `Array<Char>` in TestArray), falsifying "no legitimate clone path" and proving mechanism 4 REACHABLE on master in a single run. Fix: the delegate pointer (the one clear() swaps wholesale) and the mutability enum now live in one `ArrayState` cell shared by reference across all cloneAs views; `isMutable()` derives from the cell; `ArrayHandle` earns the `supportsMutableViews()` opt-in (the GenericHandle cell precedent); the public `m_hDelegate`/`m_mutability` fields became accessors across all callers. `ArrayViewGuardTest` rewritten into the sharing proof (delegate swap through one view authoritative for all; freeze through one view authoritative for all; delegates still refuse view cloning entirely), red-verified against a simulated per-view-cell shape. Submissions row 12 upgraded from latent to proven-reachable. **TupleHandle and FunctionHandle graduated to the structural closure 2026-08-25 (same day, second wave):** the freeze-cell mechanism was HOISTED from GenericHandle into ObjectHandle as the one designed-in mutable-view mechanism - base cloneAs installs the shared cell for every supportsMutableViews() class before the first view copy, isMutable()/setMutable() route through it, and ArrayHandle overrides the install hook to a no-op (its ArrayState already shares richer state). Tuple element storage is never swapped post-construction and function bound/delegate state was always shared, so the flag was the only split axis for both; their fail-loud guards are deleted and both opt in. `TupleViewGuardTest` rewritten to the sharing proof (freeze through one view authoritative for all), red-verified against a simulated per-view shape; the view battery (Array/Tuple/Freeze/Ref/Atomic/GenericHandle/ObjectHandle guards + CloneCensusTest) and the full 22-module sweep green. The clone-view family is now structurally closed for every handle class - no fail-loud view guard remains except the deliberate base default-deny for unreviewed future classes and the array-delegate refusal. |
| Study done 2026-08-24; verdict: facades not needed | Clone eradication feasibility: restrict `cloneAs` to immutable handles; delegating facades for mutable views | `clone-usage-audit.md`, "Clone Eradication Feasibility Study (2026-08-24)" | Five desync mechanisms plus the residual row above all stem from one design choice: mutable views are shallow copies carrying private copies of lifecycle state while sharing storage. Every new per-instance field is a latent desync, and each fix is a per-class patch (FreezeCell, InitGuard, two fail-loud guards, a ratchet test pinning 24 write sites). Cloning immutable handles (ConstHeap relocation) is the one provably safe family - frozen state cannot desync. | Study shipped 2026-08-24 (all 88 handle classes classified; every cloneAs site and override read; JIT coupling proven zero). Verdict: cells + fail-loud guards have closed every discovered gap; the facade redesign's real cost is the field-encapsulation refactor it presupposes, and nothing compounds by waiting. As part of the study the unguarded `DelegateHandle` family (per-instance `m_cSize`/`m_mutability`, storage pointers replaced on grow) got its own refuse-always guard, pinned in `ArrayViewGuardTest`. Follow-up landed same day (commit `db4ae7900`): default-deny `cloneAs` for `isMutable()` handles at the `ObjectHandle` base with the explicit `GenericHandle` opt-in; the full unit suite and `xdk:installDist` are green with the deny active, and `ObjectHandleViewGuardTest` pins it. Two findings graduated to rows below (SocketHandle, FullyBoundHandle). |
| Fixed in this branch (2026-08-24, shared holder; commit `3785afdc8`) | `xRTSocket.SocketHandle` writes its per-view `socket` field through per-call `revealOrigin()` clones | `clone-usage-audit.md` study section | The service handle is constructed masked (`xRTSocket.java:91`); every native entry calls `requireSocketHandle` -> `revealOrigin()` (`:417`) -> `cloneAs(f_clzInception)` per call (`ClassComposition.java:196`); `socket` is the handle's own volatile Java field written at `:256`/`:398` on whichever view the call holds, while handle circulation (registered service handle vs returned view) decides who ever sees the write. Per-call-clone mechanism verified from source; runtime damage needs a trace. | Traced end to end and fixed. The circulation: `hTarget` is never re-resolved to the registered service handle (`xService.invokeNative*` casts the caller's handle; `ServiceContext.getService()` is never consulted for dispatch); `getCanonicalType()` is the masked `net.Socket` type, so `requireSocketHandle`'s `revealOrigin()` manufactured a fresh inception clone per entry - the connect write landed on that clone (which `:257` returned, so reads/writes agreed only by accident), the REGISTERED handle kept `socket == null` forever, and any additional access view would drop the close write silently. The socket now lives in a holder shared by every view (the `xRTServer.HttpServerHandle` idiom); two-view red/green test in `SocketHandleStateSharingTest`. Reachability confirmed: net templates are auto-discovered, `network` is an injected resource, exercised by `TestTcpClient.x`. The separate mask-escape follow-up row below is now also closed. |
| Done in this branch (2026-08-25) | `xRTSocket.finishConnect` returns the revealed inception view, defeating the `net.Socket` mask | clone study trace, `fixed-in-this-branch.md` | `requireSocketHandle` un-masks via `revealOrigin()` and `assignValues` at `xRTSocket.java:257` returned that unmasked view to application code, so the `maskAs(net.Socket)` established by `getCanonicalClass()` never reached the app. | `finishConnect` still installs the Java socket through the revealed native view, but now returns the originally constructed masked handle to application code. Keep `SocketHandleStateSharingTest.finishConnectReturnsMaskedApplicationHandle()`, which fails on the old return shape, plus the two-view shared-holder test. Cross-context native calls still call `requireSocketHandle(...)` on the app handle and reveal internally. |
| Done in this branch (2026-08-24, commits `25371b397` + `0af827c72`) | Retire `Object.clone()` in the structure family: copy constructors for the nine sealed structures | `clone-usage-audit.md`, "Retiring java.lang.Cloneable Entirely" | The Component clone family produced this branch's real clone bugs (the `Annotation` parameter fossilization and the `MethodBody` owned-copy self-target that broke the XDK build). The sealed, all-final tree is exactly nine structures, `Parameter.copyFor` already exists, and bifurcation/adoption tests are in place, so the audit's minimum replacement - a construction operation taking target parent and pool up front, declaring every field as value, owner-cache, or rebuilt-by-registration - is now ~1-2 weeks. `Token`/`Source` copy constructors ride along (trivial). The compiler `AstNode` island and `ObjectHandle` behind default-deny stay as documented islands per the feasibility verdict. | Nine copy constructors replacing `Component.cloneBody()`'s `Object.clone()`; a static `Component.copyOf` as an exhaustive pattern switch over the sealed tree so a new structure kind without a copy constructor is a compile error; remove `Cloneable` from `Component`/`MethodStructure`/`Token`/`Source`; keep `ComponentBifurcator` and adoption behavior byte-identical under the existing tests plus per-structure copy tests. |
| Optional follow-up (deliberately not done) | Seal the `TypeComposition` root | `sealed-hierarchy-audit.md` census | `DelegatingComposition` is sealed and every concrete composition is final (`6ba9dc8a2`), but the root interface stays open: `GenericHandleCloneAsTest` and `OwnershipDiagnosticsTest` implement it as synthetic view factories whose precision-tuned fakes would need a full rework to move behind a permitted adapter. | Sealing the root costs rewriting both test harnesses against a real composition (system-modules-gated boots replacing the cheap fakes) and re-proving their regression precision; do it only if a composition-kind dispatch site ever materializes that needs the exhaustiveness. |
| Must fix, open (master bug, design-level; NOT a Category A quick extraction) | Issue #541: permanent injector deadlock - fibers waiting on the shared per-container injection chain can form a wait cycle that never resolves (0% CPU, permanent hang) | `541.md`, https://github.com/xtclang/xvm/issues/541 | Genuine master bug: concurrent, but reachable in normal single-JVM usage under load - no parallel containers or reuse required. The fix is design-level, not a hunk: the runtime must detect the wait-graph cycle rather than paper over one edge of it. | Wait-graph cycle detection in the runtime: trigger on a pool going idle while fibers still wait, walk the fiber wait graph, and complete one victim request with a Deadlock exception naming the cycle. Per-container injection caching and xunit @Concurrent hygiene are mitigations only, not the fix. X-ray slice 2 is the diagnostic backbone (snapshot the hung reproducer; the fiber cycle prints itself). |
| Fixed in this branch (2026-08-24, tail-append + null guards; commit `c621b1dca`) | `xRTFunction.FullyBoundHandle` mutates `m_next` while claiming argument-derived immutability | `clone-usage-audit.md` study section | `isMutable()` derives from the bound arguments (`xRTFunction.java:889-898`), but `chain()` writes `m_next` post-construction (`:939`): an all-immutable-args chain node passes the mutable-view refusal guard yet mutates, so the immutability claim is wrong-shaped independent of views (finalizer chains are runtime-internal today). | Traced: every handle is created fresh per constructor invocation, lives only in `Frame.m_hfnFinally` and a continuation-local list, is consumed immediately on the constructing fiber, and is never stored, marshalled, cloned (`revealOrigin` returns `this`), or frozen - so `m_next` mutation is fiber-local and benign. Two shape defects fixed: the unsound `assert m_next == null` in `chain()` (reachable via an annotation-mixin constructor with a finalizer delegating to a super constructor with one; -ea asserts, -da silently dropped the linked finalizers) is now a tail-append; and `isMutable()`/`makeImmutable()`/`checkArgumentsPassThrough()` skip the register-file null padding in `f_ahArg` instead of NPE-ing if a freeze/pass-through path ever reaches a finalizer handle. |
| Closed 2026-08-24 as an audit (two graduations below) | Mutable array element exposure, repo-wide | `array-element-exposure-audit.md` | Java arrays are mutable and every raw share is a potential cross-owner write channel. | 105 public/protected array fields, 358 array-returning methods (21 strict raw-return accessors), 12 retained-without-copy constructor families, 28 non-empty static tables (zero writes found), 30 empty sentinels, plus 21 live-collection getters and 8 getters whose `Collections.unmodifiable*` wrapper exists only under `-ea`. 10 DELIBERATE protocols and 10 LATENT hazards classified from site reads; 2 BUG CANDIDATES graduated below. Transition verdict: staged, not a rewrite - two hunk-sized bugs, the 8 assert-only wrappers, ~20 cold copy conversions, then one wide `FrozenArray`-style change for the interned-metadata families; hot runtime currency (registers, field arrays, delegates, Ctx, bridge ABI) deliberately never moves. |
| Fixed in this branch (2026-08-24, reflection path; ISA tuple-arg op decision still open) | Reflection `Method.invoke` turns the caller's tuple storage into the callee register file | `array-element-exposure-audit.md` | `xRTMethod.invokeInvoke` (xRTMethod.java:211, carrying an in-code `TODO GG+CP do we need to check these?`) passes `hTuple.m_ahValue` into `CallChain.invokeT`; `Utils.ensureSize` returns the array unchanged when the target needs no extra registers, so the tuple's storage becomes the callee `Frame.f_ahVar`, and any parameter reassignment mutates the caller's - possibly immutable, possibly const-heap-cached - tuple. Master bug, byte-identical on master, single-threaded reachable. xRTFunction.java:254 clones in exactly this case, proving the hazard was known. Seven tuple-arg ISA ops share the shape but are latent (today's compiler never sets `m_fTupleArg`; the opcodes still decode from any .xtc). | Shipped 2026-08-24: `xRTMethod.invokeInvoke` clones `hTuple.m_ahValue` before it reaches `CallChain.invokeT` (mirrors `xRTFunction.invokeInvoke`; the in-code `TODO GG+CP do we need to check these?` is answered and removed). Pinned by `MethodInvokeArgumentAliasingTest`: the `ensureSize` pass-through contract, a shape pin on the method path (red on master), and a shape pin keeping the function path's copy. The ISA tuple-arg decision landed 2026-08-25: all eleven tuple-argument ops (`Call_T0/T1/TN/TT`, `Invoke_T0/T1/TN/TT`, `Construct_T`, `New_T`, `NewG_T`) now extract through `TupleHandle.valuesCopy()` instead of aliasing `m_ahValue` - a latent-path-only copy, since today's compiler never emits the tuple-arg forms but the opcodes decode from any `.xtc`; pinned by `MethodInvokeArgumentAliasingTest.tupleArgumentOpsCopyTupleStorage()` (red on the raw-extraction shape). Still open from this row: the full execution test (invoke reflectively, reassign a parameter, assert the caller's tuple unchanged) rides with the same-JVM stress harness. |
| Fixed in this branch (2026-08-24) | Short-hand property methods share the super method's `Parameter` elements cross-module | `array-element-exposure-audit.md` | `MethodDeclarationStatement.java:521` builds a new method from `methodSuper.getReturn(i)` `Parameter` objects - usually owned by the loaded ecstasy/library `FileStructure` - via the aliasing `createMethod`; `Parameter.registerConstants` (Parameter.java:408-413) then rewrites the shared element's constants into the user module's pool at assembly. Master bug, single-threaded compile reachable; only the `Referent`-terminal case is defended today. The fix machinery already exists in this branch (`createMethodCopyingParameters` / `Parameter.copyFor`, the row-103 family). | Shipped 2026-08-24: new public `Component.createMethodCopyingParameters(...)` (delegates to the row-103 `MultiMethodStructure` copying factory) and the short-hand property block in `MethodDeclarationStatement` now routes through it. Pinned by `ComponentMethodParameterCopyTest`: cross-module element copy (borrowed library `Parameter` stays parented in the library module), registration isolation (`registerConstants` on the copy adopts into the user pool while the library element keeps its own pool), and a call-site shape pin (red on master). `xdk:installDist` green with the change. |
| Stage 1 done in this branch (2026-08-24); stages 2-3 and `TypeInfoReal` maps open | Array/collection exposure hardening stages 1-3 plus `TypeInfoReal` getter maps | `array-element-exposure-audit.md` | Read-only contracts that exist only by convention: 8 getters whose `Collections.unmodifiable*` wrapper is created inside an `assert` and evaporates under `-da` (Component, ListMap, MultiMethodStructure, FileStructure, ModuleStructure, CompositeComponent); ~20 cold paths that can copy or `List.of` cheaply; 4-5 interned-metadata families protected only by a hand-maintained clone-before-write convention; `TypeInfoReal` getters hand out the live internal `HashMap`s. | Stage 1 shipped 2026-08-24: all 8 wrappers unconditional (`Component.getContributionsAsList/children/getTypeParams`, `CompositeComponent.components`, `MultiMethodStructure.methods`, `FileStructure.moduleIds`, `ModuleStructure.getFingerprintVersionPrefs`, `ListMap.asList`), pinned by `ReadOnlyViewContractTest` - mutation-through-accessor throws with or without `-ea`, and a shape pin bans the `assert (x = Collections.unmodifiable...)` idiom repo-wide (red on master, 8 hits). `TypeInfoReal` getters hardened 2026-08-25: `getProperties`/`getVirtProperties`/`getMethods`/`getVirtMethods`/`getTypeParams` return `Collections.unmodifiableMap` views, `getContributionList` an unmodifiable list, and the two annotation-array getters return clones - verified safe because every reader is read-only (audit) and the ownership helpers rebuild fresh maps at construction, so the fed-back getter results in the merge/annotation construction paths are only iterated; pinned by `TypeInfoMemberOwnershipTest.memberMapGettersAreReadOnlyViews()` (red on the live-map shape). Deferred from that wave: the three `ListMap`-typed getters (`getClassChain`/`getDefaultChain`/`getChildInfosByName`) need a `TypeInfo` interface retype to `Map` plus a ~10-site caller sweep before they can wrap. Stage 2 shipped 2026-08-25: `Version.getIntArray`, `ErrorInfo.getParams`, `EvalCompiler.getArgs`, `ClassTemplate.getImplicitFields`, and `ArrayConstant.getValue` return copies; `ChildInfo.getAllIdentities` and `Component.Injector.getInjections` return unmodifiable views (closing the boarded `ChildInfo.f_setIds` exposure); `LongLong.divrem` returns fresh result pairs instead of the shared `ZEROx2` array (constant deleted); the `PackedInteger` factor tables are immutable `List.of`. Documented deviation: the constant byte-payload accessors (`UInt8ArrayConstant.getValue` et al.) stay zero-copy BY DESIGN - `xByteArray` materializes Constant-mutability delegates directly over the interned bytes, and the mutability protocol guards writes; copying there would tax every constant array materialization. Stage 3 open: `FrozenArray`-style wrapper per metadata family, family-sized PRs against master. All master-portable per the audit's verdict. |
| Stages 0-4 done in this branch (2026-08-24); Op/AstNode/TypeComposition deferred | Sealed-hierarchy adoption, stages 0-4 | `sealed-hierarchy-audit.md` | Zero `sealed` declarations today versus 1,549 `instanceof` and 167 discriminator switches - 39 of whose defaults silently produce a value (`TerminalTypeConstant.isTuple()` makes any new format silently 'not a tuple'; the `ClassConstant` namespace walkers make unknown identity kinds silently 'outermost'; `Builder.checkNull/checkNotNull` emit unverifiable JIT bytecode behind an assert) - plus 49 same-subject instanceof cascades, and `NameExpression.getMeaning():2927` needs a fallthrough suppression purely because the hierarchy is open (a direct collision with this branch's fatal gate). JDK 25 toolchain, unnamed module: permitted subclasses must share the root's package. | All five stages shipped 2026-08-24 as separate commits. Stage 0 `e59d4f82d`: ConditionalConstant/PseudoConstant/FrameDependentConstant/TypeInfo sealed, SimulatedLinkerContext demonstrator. Stage 1 `dc39387bd`: BinaryAST/ExprAST (52 classes), NodeType factory exhaustive with the ReturnTStmt hole explicit and tested (SealedAstFamiliesTest), readExprAST corrupt-stream boundary. Stage 2 `298067019`: TypeConstant (21 classes). Stage 3 `cace6570a`: IdentityConstant with ZERO hatches (the constructor-escape probe fakes were retired - the fatal this-escape gate enforces their property - so PropertyConstant/FormalTypeChildConstant seal properly) + ValueConstant. Stage 4 `07ed937b3`: Component tree. Hatch closure `ab5788584`: the last three non-sealed hatches (StringConstant, MethodStructure, PropertyStructure) are final - the deadlock latch extends the Constant root directly and the structure fakes were replaced by same-package protected access, so every sealed family is fully closed. Payoff rewrites `78111b85f`: ClassConstant walkers and NameExpression.getMeaning are exhaustive pattern switches - fallthrough suppression, empty default, and trailing ISE deleted; silent-terminator arms now explicit. DefiningConstant union shipped and TerminalTypeConstant converted (`86c89e175` + `bb72c4b58` + `10ba2869d`): 22 of 23 format switches and all 48 defining-constant casts retired without the 150-call-site return-type change (checked union conversion at the file boundary instead); the one survivor (`:1547`) is the keyword-kind data switch, verified by broad grep. Still open, opportunistic cascade conversions, `Op` (package split), compiler AstNode (backlog), TypeComposition (test adapter). Every wave pinned by reflection tests; suite + xdk:installDist green per stage. |
| Done in this branch (chain of four) | XDK build was broken at branch HEAD; `xdk:installDist` is green again | `runtime-ownership-hardening-ledger.md` rows 31-32, `fixed-in-this-branch.md` | Four independent branch regressions masked each other because nothing had run a full XDK build since the adoption/owner waves landed: (1) `Annotation` constructor param-array copy fossilized compiler placeholder params (bisected to `4089dc728`, single hunk); (2) fail-closed `NativeRebaseConstant` adoption broke every module downstream of ecstasy; (3) `EnumValueConstant` adoption re-ran a shape check that NPEs mid-merge; (4) `MethodBody` owned-copy fabricated self targets for fresh bodies (bisected with fix overlays to `93189541f`), corrupting union/difference TypeInfo merges into the lib_json COMPILER-177 access failure and causing the earlier equality stack overflow. | Keep the adoption/aliasing regression tests (`ConstantAdoptionTest`, `MethodInfoTest.owningFreshBodyDoesNotFabricateSelfTarget`), and add a CI-visible `xdk:installDist` (or at least `xdk:lib-json:compileXtc`) gate to this branch's workflow so compile regressions can never hide behind unit-green suites again. |
| Should fix (wave, post-gate) | Migrate statement switches to arrow-form `switch` where no cascade is needed | fallthrough gate in `org.xtclang.build.java.gradle.kts`, row 133 classification | Arrow-form `case X ->` cannot fall through, so the hazard class is defined out of the language instead of policed by the gate. Each migration shrinks the `@SuppressWarnings("fallthrough")` list and removes a review obligation. | Migrate opportunistically when touching a file; keep genuine cascades (operand-swap, contribution walks, parser precedence chains) as gated statement switches. |
| Done in this branch (2026-08-25) | Remove `ConstantPool.withPool(...)` completely | `ambient-context-audit.md`, `constant-pool-state-audit.md` | The scoped bridge hid an owner in dynamic thread state even after every semantic reader was converted to explicit owners. The removal audit confirmed `s_tloPool` had NO remaining semantic readers - only `withPool` itself and the two transitional assertions consulted it, so every scope was self-referential scaffolding (installing the very pool it then asserted). | Deleted: `withPool(...)`, `s_tloPool`, `assertCurrentPool`, `assertCurrentPoolIfPresent`, `VALIDATE_CURRENT_POOL_PROPERTY`, and all 21 main-source scopes (unwrapped to their bodies; try-with resources that carried catch/finally became plain try blocks). The five bridge-API tests are gone; the ambient-immunity tests keep their assertions minus the now-inexpressible hostile-ambient setup, and the immunity property is structural (no ambient state exists). Reintroduction is pinned away by `currentPoolLookupGetterDoesNotExist()` reflection checks plus the repo-wide `getCurrentPool(` source scan, which now covers `ConstantPool.java` itself. |
| Should fix (near end of should queue) | Side-effect-free `toString()`/display path redesign | `plans/side-effect-free-tostring.md` (inventory + per-site replacements), `ownership-diagnostics.md` deferred-cell precedent | Many `toString()`/`getValueString()`/`getDescription()` implementations force lazy caches, build TypeInfo, resolve types, or intern pool constants. Rendering a variable in a debugger mutates the state under investigation, which makes race debugging nondeterministic and has repeatedly required hand-patching display code mid-investigation. | Land the plan's contract (pure `toString()` over already-computed state with deferred markers; explicit forced-dump variants), migrate per the inventory, and add the banned-callee source-shape gate. |
| Slice 1 done in this branch; slices 2-5 open | Complete world-state snapshot ("X-ray") diagnostics | `ownership-diagnostics.md`, `plans/same-jvm-launcher-stress.md` | Ownership sweeps are only as good as the tree they walk. A complete, structured snapshot of all containers, services, fibers, frames, handles, pools, and type/metadata caches — sweepable for foreign-owner references the way stress tests already sweep partial state — is the verification backbone for reentrancy claims. | Slice 1 shipped: `OwnershipDiagnostics.snapshotWorld(Runtime)` returns a structured `WorldSnapshot` (all registry containers + full cross-set validation), renderable and diffable; `WorldDiff.retained()` is the sequential-run leak signal, and native containers now register post-construction so the world enumeration is complete (`WorldSnapshotTest`). Demo/teaching layer added 2026-08-24: WorldSnapshotDemoTest prints runnable sequential- and multi-container world dumps, the diff with the retained-container leak signal, and the ownership-sweep verdict (run with -Porg.xtclang.java.test.stdout=true). Slice 3 shipped 2026-08-25: `OwnershipDiagnostics.sweepForeignReferences(Container)` is the generic reflective reachability sweep - BFS over every field, array element, and collection entry of the container's reachable state, flagging owner-scoped objects (mutable handles, templates, compositions, contexts, `HandleConstant` live handles in reachable pools) whose owner is unrelated (not self, ancestor, or descendant), with field-by-field path-to-root evidence and no silent truncation. Documented boundaries: other containers and foreign contexts are flagged but not entered (sweep each separately); immutable handles are legitimate pass-through currency and are leaves; the constant plane is owner-neutral except the live-handle check; statics are not enumerated (source-shape scans + the parked JIT rows own those); reachability proves retained references, not temporal races (stress owns those). `OwnershipReachabilitySweepTest` is the detector's own red/green harness: foreign plants at every structural depth must each be found with a correct path, so 'can the detector detect everything' is measured, not assumed. Stress-harness sweep wiring shipped 2026-08-25 and iterated to green: `IsolatedDirectExecutor.validateRuntimeOwnership` now also runs `sweepForeignReferences` on every windowed container and fails the run on violations OR blind spots; the iterate-to-green loop surfaced and fixed, in order - the reachable mechanism-4 array-view path (row above), the pool-scoped-vs-thread-scoped synthesis-window miscalibration (fiber type calculus interns into other modules' pools), the guard-diagnostics recursion (formatting a violation re-entered the type calculus), the diagnostics-masking crash (the forensic dump itself tripped the guard, replacing the real finding), and two validator strictness gaps versus documented runtime policy (composition lazy state may hold native-parent immutable currency; parent-flow const sharing between RELATED containers of immutable/ownerless handles is by design - `isLegitimateSharedCurrency`). `runDirectSequenceStress` (2 iterations, TestArray+TestReflection) passes with curated validation plus full sweep enforced. Remaining slices: execution-state capture (fibers/frames/registers, quiesced mode) and the pool/compiler planes. |
| Should fix (compiler/LSP blocker) | Request-owned diagnostic authority; stop deep `ErrorList` creation and unexplained `BLACKHOLE` suppression | `logging-diagnostics-audit.md` ("Direct `ErrorList` And `BLACKHOLE` Findings") | Main code has ~80 `ErrorListener.BLACKHOLE` uses and 10 direct `new ErrorList(...)` sites. The bad pattern is not the list class itself; it is that parsers, type-info builders, runtime reflection helpers, JIT/linker code, and eval compilation can allocate or suppress diagnostic listeners far below the entry point that knows request id, document version, phase, owner, container, and source context. | Introduce a request-owned `DiagnosticSession`/`DiagnosticSink`; make `ErrorListener` a bridge, not the primary authority; require a documented probe/branch reason for suppressed speculative diagnostics; replace null-listener-to-`BLACKHOLE` adapters at public/compiler boundaries; add tests that assert structured diagnostic fields before rendered text. |
| Should fix (API discipline wave) | Nullness and absence contracts | `nullness-annotation-audit.md` | Main Java sources have ~700 `return null` sites, ~6849 null checks/assignments, and only 17 annotation hits even though JetBrains annotations are already on the compile classpath. Null currently means absent, not computed, parse failed, error already logged, listener disabled, cache empty, lifecycle cleared, or legitimate value depending on local convention. That makes IDE/nullness diagnostics noisy and blocks converting required constructor state to final owner-passed fields with confidence. | Add high-signal `@NotNull`/`@Nullable` annotations at constructor/API/owner boundaries while keeping runtime guards; return empty immutable collections where absence means "none"; use typed result/diagnostic failure instead of null after side-channel logging; classify optional lookup APIs package-by-package before adding any analyzer gate. |
| Should audit/fix opportunistically | Duplication and legacy Java control-flow patterns that duplicate invariants | `modern-java-syntax-audit.md`, `lint-parallelism-risk-audit.md`, IntelliJ duplicate-code diagnostics | Duplicated decision blocks, labelled nested loops, parameter reassignment, fallthrough state machines, anonymous visitor scaffolding, raw collections, and unchecked casts force owner/type/lifecycle invariants to be remembered repeatedly at runtime. That is how one path silently diverges when another path is fixed. | Use IntelliJ duplicate-code reports plus focused source reading to extract shared helpers or typed records where semantics match. Prefer arrow switches, pattern variables, sealed roots, typed payload records, lambdas/method references, and final locals when they reduce mutable state. Do not mechanically rewrite hot loops or constructor paths that would capture `this`; every extraction must preserve owner and allocation behavior. |
| Nice to have (do last) | Developer logging facade (`XtcLog`/`LogCat`/`LogSink`) and print-site migration | `logging-strategy.md`, `logging-diagnostics-audit.md` | Valuable for diagnosis and LSP hosting, but not a correctness fix. Deliberately parked behind all must-fix and must-audit work. | The design is fully recorded in `logging-strategy.md` (repo-owned facade, pluggable/composable sinks, SLF4J confined to one adapter, ungated reporting plane, Tier 2 constant-gated developer plane). Implement as a small standalone PR when the must-fix list is empty. |

## Audit Categories

| Priority | Category | Representative sites | Why it must be audited | Proper closure |
| --- | --- | --- | --- | --- |
| Fixed in this branch | Runtime-executed owner-bearing `Op` caches | `asm/op/JumpCond.m_cond`, `asm/op/JumpNCond.m_cond`, runtime write-back to `asm/OpTest.m_typeCommon` and `asm/OpCondJump.m_typeCommon`, `asm/op/JumpVal` and `asm/op/JumpVal_N` switch-table fields | These cached constants or handles obtained from a `Frame`. If decoded op graphs are reused across containers or constant pools, the cached value can carry the wrong owner. | The condition fields were removed, common-type execution resolves from the current `Frame`, and switch tables are cached under the executing `Container`. `OpRuntimeCacheTest` guards against reintroducing the patterns. |
| Closed 2026-08-25 for Part 1 runtime/ASM audit; structural snapshot follow-up remains | Complete `ConstantPool` ownership and mutation inventory | `asm/ConstantPool`, `asm/Constant`, `asm/constants/*`, `adoptedBy(...)`, ambient current-pool helpers, late registration, live `HandleConstant` values | This branch fixes the proven runtime-owner defects and now fences runtime-published pools against both new constants and destructive lifecycle mutation. Compiler/linker/serializer/JIT ownership remains a separate phase/snapshot design problem, not an unfixed runtime-published mutation path. | Maintain the detailed catalog in `constant-pool-state-audit.md` and the expanded background inventory in `constant-pool-hostile-state-audit.md`. Remaining proper closure is structural: explicit owner parameters, deletion of the `withPool(...)` bridge, owner-local concurrent caches, immutable runtime snapshots, and compiler/JIT phase confinement.. SNAPSHOT CLASSIFICATION LOCKED 2026-08-25 ("Post-Publication Residual Audit" in `constant-pool-state-audit.md`): the snapshot's READER-SAFETY half was MUST FIX and is CLOSED 2026-08-25 (ledger row 58): a volatile runtime read mirror (`m_aconstMirror`/`m_cconstMirror`, monitor-written, acquire-read count-then-array) now backs `getConstant(int)`/`size()`/`getConstants()` once a pool is runtime-published - an index below the count always resolves to a fully published element, and a racing index falls back to the pool monitor; the completion gate is unchanged. The frozen-base + runtime-annex + upstream-guard-scope architecture remains the SHOULD half. |
| Done for first publication; must audit for full frozen-code lifecycle | Runtime `Op` address/link caches | `asm/OpJump.m_opDest`, `asm/OpCondJump.m_opDest`, `asm/op/LoopEnd.m_opDest`, `asm/op/OpSwitch.m_aOpCase`, `asm/op/JumpInt.m_aOpCase`, `asm/op/GuardStart.m_aOpCatch` | Mutable decoded instruction links are safe only if resolution happens before runtime exposure. This branch now links before `getOps()` returns and validates readiness under `xvm.asm.validateRuntimeCode`. | Keep the diagnostics and source-shape tests. A later PR should separate mutable assembly `Code` from immutable runtime `ResolvedCode` instead of treating all `Code` lifecycle states as runtime-ready.. FULL-LIFECYCLE AUDIT CLOSED 2026-08-25 (see "Full-Lifecycle Completion Audit" in `runtime-metadata-op-cache-classification.md`): 332 fields/819 assignment sites swept; all prior DONE rows verified intact. Residue classified: the overstated PROVEN-CONFINED row corrected - JIT builds write owner-bearing, non-convergent state (`Label.m_label`, `Guarded.m_scope`, `GuardAll.m_jumps`, finally-jump ints) into MODULE-SHARED decoded ops, MUST FIX before JIT concurrency (folded into issue rows J23/J24); `forceAssembly`-after-publication and `markNative` transitions stay ordering-by-convention - remaining must-audit follow-up is a writer-side diagnostic; `OpInvocable.m_constMethod` runtime write deleted same day; `m_cVars` skip-path and `m_ast` lazy decode are narrow JMM should-fixes; four convergent MethodStructure lazy caches are JMM-hygiene should-fixes; everything else safe-documented. |
| Closed 2026-08-25 for runtime/ASM metadata; JIT/compiler/should-fix backlog remains | Type and constant metadata caches | `asm/ConstantPool` volatile maps and thread-local pool, `asm/constants/TypeConstant` volatile caches and `ScopedValue`, `asm/constants/TypeInfoReal` maps | These are central owner-bearing metadata structures. Some use synchronization already, but same-JVM incremental compile/runtime reuse depends on exact ownership. Runtime boundary `ConstantPool` scopes are now asserted, `ClassComposition` field layout is now an inception-owned final lazy holder, runtime publication is now always marked before user code, and the remaining runtime `TypeConstant` normalized cache is safely published. | Keep the owner/key/invalidation table in `runtime-metadata-op-cache-classification.md`. Remaining work is explicit-owner cleanup (`withPool(...)`), immutable `ChildInfo` identity exposure, compiler request-owned metadata, and JIT `TypeSystem`/classloader-owned cache policy. |
| Classified 2026-08-25; compiler-scoped record-only | Compiler AST and context mutation | `compiler/ast/Context.m_mapByName`, `NameResolver.m_constant`, `InvocationExpression.m_method`, `LambdaExpression.m_lambda`, statement break/continue lists | The compiler historically assumes request-local AST mutation. The audit read each named family: context name maps, staged name resolvers, invocation method caches, lambda method structures, and break/continue assignment lists are transient validation/codegen state owned by a compiler request under today's model. Incremental and parallel compilation makes that assumption dangerous unless request ownership is explicit, but no runtime/ASM owner leak graduated. | Keep the evidence in `compiler-lexer-parser-this-escape.md`. Do not fix on this runtime branch. The compiler-reentrancy branch must either prove one AST/context graph per request, move scratch caches to request-owned context state, or rebuild/clone ASTs per concurrent validation pass. Separate known global counters remain on `lagergren/compiler-counter-atomics`. |
| Closed 2026-08-24 except delegated counters | Non-final statics | The four compiler counters; `javajit/Ctx.MD_inject`; JIT builder field-name strings | Non-final statics are shared across every compile/runtime in the JVM and usually have no reset story. | Done: `Ctx.MD_inject` and the three JIT builder name strings were immutable values missing `final` and are now `static final`; the repo-wide scan is clean apart from the four compiler label/lambda counters, whose closure is the separate `lagergren/compiler-counter-atomics` PR (non-atomic increments are single-threaded-compile-confined today and wrong for parallel compilation). |
| Classified 2026-08-24 (see Row 134 Completion Sweep) | `ThreadLocal`, `TransientThreadLocal`, and `ScopedValue` | `ambient-registry-must-audit-classification.md` covers `ConstantPool.s_tloPool`, `f_tlolistDeferred`, `ServiceContext.s_tloContext`, `MultiMethodStructure.s_tloIgnoreNative`, `TypeConstant.s_context`/`m_tloInProgress`, `TypeParameterConstant.f_tloReEntry`, `TransientThreadLocal`, and `javajit/Ctx.Current` | Ambient context hides dependencies from APIs. ThreadLocal state also leaks across pooled threads if cleanup is missed. | Every mechanism is classified with set/clear-discipline evidence; no unguarded set-without-clear exists in the ThreadLocal class. Remaining explicit-owner conversions ride the named slices (JIT ambient `Ctx` rows, ServiceContext diagnostic readers, compiler-side `withPool` scopes). Prefer explicit owner parameters for permanent APIs; `ScopedValue` stays a bounded lexical bridge. |
| Closed 2026-08-24 as an audit (one graduation to must fix) | Weak/identity maps | `state-inventory.md` ("Row 160 Weak/Identity Registry Sweep") | These maps often encode identity ownership or lifecycle. They are not concurrent by default and weak keys can disappear at surprising times. | 20 registry rows audited: 10 CONFINED with evidence (`f_mapOpInfo`/`m_mapTransient` under the scheduling-lock ownership protocol with no cross-service reader; `f_setValidPools` compile-thread-built then frozen; `OwnershipDiagnostics` maps per-Dumper), 9 SYNCHRONIZED with named monitors (incl. the fixed `Runtime.f_containers`; two flagged JIT `Xvm` registry windows routed to the JIT rows), and 1 NEEDS-FIX graduated to the must-fix row below. Bonus finding: `MethodStructure.Code.m_mapIndex` is a dead write-only field (only nulled/alias-copied) - removal is a should-fix cleanup. |
| Closed 2026-08-25 (one should-fix cleanup included) | Java `clone()` / `Cloneable` ownership paths | `MethodStructure.cloneBody()`, `ObjectHandle.cloneAs(...)`, AST/component clones; `Constant.adoptedBy(...)` as fixed precedent | The focused audit found clone paths that can preserve original owners, share method/register state, or shallow-copy runtime handles. The `Parameter` source-mutation, `MethodStructure` copied-owner, delegated-parameter sharing, `Constant` default-adoption clone, `GenericHandle` view backing, socket view state, and socket mask-escape bugs are fixed here. The completion sweep verified the current `javatools/src/main/java` surface: live `Cloneable` implementors are only `AstNode` and `ObjectHandle`; `super.clone()` remains only in the compiler AST island and guarded runtime handle island; `.clone()` leftovers are compiler scratch copies, guarded handle views, or array-container copies. | Keep `clone-usage-audit.md` row-179 completion sweep, `ComponentBodyCopyTest`, `AsmConstructorEscapeTest`, `GenericHandleCloneAsTest`, view-guard tests, socket tests, and constant-adoption tests. The stale branch-internal `Component.Contribution.clone()` override is removed and pinned by `ComponentBodyCopyTest.contributionDoesNotDeclareCloneOverride()`. Remaining work is not a runtime/ASM must-fix: compiler AST scratch-copy redesign belongs to the compiler branch; JIT bridge `nLongBasedArray` clone is recorded for JIT-specific cleanup. |
| DONE 2026-08-27 (board was stale - all already frozen: ALREADY_DISPLAYED is ConcurrentHashMap.newKeySet, Token.KEYWORDS/ALL_KEYWORDS/PREFIXES + NativeNames.reservedMethodName are Map.copyOf, wait-frame op singletons already carry the stateless-safe comment) | Freeze the last mutable statics; comment the wait-frame op singletons | `BinaryAST.ALREADY_DISPLAYED` (:283, runtime-mutated debug dedupe set - the only static collection still written at runtime, unsynchronized raw `HashSet`), `Token.KEYWORDS`/`ALL_KEYWORDS`/`PREFIXES` (:732-765, clinit-only), `NativeNames.reservedMethodName` (:21, clinit-only), `Frame.WAIT_FOR_FUTURE`/`WAIT_FOR_IO`/`Utils.WAIT_FOR_RELIEF` (stateless anonymous `Op` singletons, re-verified no fields and no assembly-mutator calls) | Debug-only or clinit-published state; no cross-owner path found, but the discipline is comment- and convention-enforced. | Convert `ALREADY_DISPLAYED` to `ConcurrentHashMap.newKeySet()`; freeze the four clinit maps with `Map.copyOf`/`Map.ofEntries` and make `reservedMethodName` private; add the "stateless process-wide singletons - assembly-time mutators must never run on these" comment to the wait-frame ops. `NO_*` sentinels re-verified zero-length; the 28 non-empty static tables keep their cosmetic `List.of` conversion on the API-cleanup list. |
| Split 2026-08-25 (audit): document + privatize; freeze family already closed | Public/protected mutable fields | `Frame` public execution fields, `ObjectHandle.m_clazz`/`m_fMutable`, `DelegateHandle.m_cSize` and delegate storage pointers, compiler AST protected fields | Audit findings: `Frame` execution fields have a REAL enforced confinement invariant - a context executes only while holding the scheduling lock (VarHandle CAS acquire/release in `ServiceContext`), giving mutual exclusion plus happens-before across carrier threads - but it is documented nowhere, and `DebugConsole` performs tolerated racy cross-context frame reads and `m_debug` stash writes. `m_clazz` is ctor/clone-assign-only; `m_fMutable` freeze transitions are the already-closed FreezeCell/default-deny family (`FreezeViewSharingTest`, `ObjectHandleViewGuardTest`, `GenericHandleCloneAsTest`); mutable delegates cannot cross service boundaries (`isPassThrough`), and the delegate view-clone fork path is closed refuse-always. Compiler AST protected fields are request-confined per the compiler-branch row; nothing owner-bearing reaches runtime types. | Closed 2026-08-25: (1) the scheduling-lock confinement contract is written into `Frame.java`'s class javadoc, naming the tolerated `DebugConsole` display-only races; (2) `ObjectHandle.m_clazz` is private (writes compiler-confined to construction and `cloneAs`; the five stray direct reads now go through `getComposition()`); (3) `m_fMutable` privatization is deliberately NOT done - the fatal `-Xlint:this-escape` gate flags even final-method calls from subclass constructors, so the 21 constructor initializations must stay direct field writes (the field's javadoc records this), and the constructor-only discipline stays pinned by `FreezeViewSharingTest.mutabilityFlagWritesAreConstructorOnly()`; (4) `DelegateHandle.m_cSize` and the typed storage pointers stay public deliberate hot-path struct fields (216 sites) with the Frame-style confinement contract documented on `DelegateHandle` - privatizing them would contradict the array audit's hot-runtime-currency verdict. |
| DONE 2026-08-27 (board stale - 0 anonymous-array argument sites remain) | Explicit anonymous arrays in expression contexts | `createFrameN(..., new int[] {...})`, `ensureSignatureConstant(..., new TypeConstant[] {...})`, `ensureFileConstant(..., new byte[] {...})`, one-element `ObjectHandle[]` varargs bridges | Java array initializer sugar only works when the target array type is explicit, so method-argument sites keep ugly `new T[] {...}` unless the API offers a typed helper/overload. This is mostly readability, but owner-heavy code benefits from naming argument arrays such as return registers or file contents. | Prefer typed locals with `{...}` when touching nearby code. For repeated expression sites, add small owner-preserving overloads or helper methods instead of spreading anonymous arrays through runtime code. This branch removed the explicit anonymous arrays from its added Java lines where a local/helper was clean. |

## Manual Lazy Null Caches

The focused classification of current same-field lazy-null sites is maintained
in [manual-lazy-cache-audit.md](manual-lazy-cache-audit.md).

The rule is:

- If the receiver can be shared by runtime containers, fibers, compilation
  workers, or same-JVM executions, this is `must audit`.
- If the cached value is owner-bearing and the receiver can be shared across
  owners, it becomes `must fix`.
- If the receiver is proven request/thread confined, document that proof and
  leave it out of the first runtime-owner PR.

## Stress Verification Backlog

More same-JVM direct-mode stress is useful verification depth, not a known code
hole by itself. The first PR already adds `manualTests:runDirectSequenceStress`;
the next confidence wave is to run more modules and more iterations with
ownership validation enabled:

```bash
CI=true ./gradlew :manualTests:runDirectSequenceStress \
  -PsameJvmIterations=2 \
  -PsameJvmModules=TestArray,TestReflection \
  --console=plain --warning-mode=all --no-daemon --no-configuration-cache
```

The longer plan is in
[plans/same-jvm-launcher-stress.md](plans/same-jvm-launcher-stress.md): serial
same-JVM execution first, then structured failure artifacts, benchmark output,
parallel same-JVM execution, and Gradle direct-mode integration coverage.
