/**
 * An annotation that can be applied to a test target to provide a human readable name for the test
 * fixture.
 *
 * @param  the display name to use for the test fixture
 */
annotation DisplayName(String displayName);
